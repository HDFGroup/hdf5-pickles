# Message routing for H5_RESOURCE_LINK_TRAVERSAL_DEPTH.
# Authoritative source; the loader expands each matches list and applies
# longest-substring-first precedence.
schema_version: 1
finding: H5_RESOURCE_LINK_TRAVERSAL_DEPTH

routes:
  - record: validation_controls
    invariant: control.link_depth
    scope: resource
    evidence: fixture
    matches:
      - "link traversal depth exceeds the selected profile limit"
