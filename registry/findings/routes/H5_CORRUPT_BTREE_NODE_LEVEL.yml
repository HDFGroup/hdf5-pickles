# Message routing for H5_CORRUPT_BTREE_NODE_LEVEL.
# Authoritative source; the loader expands each matches list and applies
# longest-substring-first precedence.
schema_version: 1
finding: H5_CORRUPT_BTREE_NODE_LEVEL

routes:
  # The loader orders matches LONGEST FIRST -- substring matching, first hit
  # wins.  One rule serves both v1 B-tree roles; the family comes from the
  # message because `record` selects the exact-build canary.
  - record: chunk_index
    invariant: chunk.btree_node_level
    scope: aggregate_object
    evidence: curated
    matches:
      - "chunk B-tree child node level is not one below its parent"
  - record: btree_heap_index
    invariant: btree.node_level
    scope: aggregate_object
    evidence: curated
    matches:
      - "group B-tree child node level is not one below its parent"
