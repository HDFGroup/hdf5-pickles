# Message routing for H5_RESOURCE_SINGLE_VALUE_BYTES.
# Authoritative source; the loader expands each matches list and applies
# longest-substring-first precedence.
schema_version: 1
finding: H5_RESOURCE_SINGLE_VALUE_BYTES

routes:
  # One profile budget, three different single values -- each owned by the
  # family whose message carries it, not by whichever walker happens to test
  # the limit.
  - record: datatypes
    invariant: datatype.single_value_budget
    scope: resource
    evidence: curated
    matches:
      - "datatype storage size exceeds the selected profile limit"
  - record: message_envelope
    invariant: envelope.single_value_budget
    scope: resource
    evidence: curated
    matches:
      - "attribute payload exceeds the selected profile limit"
  - record: dataset_layout_filter_fill
    invariant: layout.single_value_budget
    scope: resource
    evidence: curated
    matches:
      - "fill value exceeds the selected profile limit"
