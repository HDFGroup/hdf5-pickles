# Message routing for H5_CORRUPT_V2_BTREE_RECORD_SIZE.
# Authoritative source; the loader expands each matches list and applies
# longest-substring-first precedence.
schema_version: 1
finding: H5_CORRUPT_V2_BTREE_RECORD_SIZE

routes:
  # The loader orders matches LONGEST FIRST -- substring matching, first hit wins.
  # One shared v2-btree engine serves every role, so the family comes from
  # the message; the invariant is that family's own, because `record`
  # selects the exact-build canary.
  - record: shared_messages_legacy
    invariant: sohm.btree_record_size
    scope: aggregate_object
    evidence: curated
    matches:
      - "SOHM v2 B-tree record size is inconsistent with its type"
      - "SOHM v2 B-tree record size is zero"
  - record: heap_structures
    invariant: heap.btree_record_size
    scope: aggregate_object
    evidence: curated
    matches:
      - "dense attribute creation-order v2 B-tree record size is zero"
      - "dense attribute v2 B-tree record size is zero"
  - record: dense_index
    invariant: dense.btree_record_size
    scope: aggregate_object
    evidence: curated
    matches:
      - "dense link creation-order v2 B-tree record size is zero"
  - record: chunk_index
    invariant: chunk.record_size
    scope: aggregate_object
    evidence: curated
    matches:
      - "chunk index v2 B-tree record size is zero"
  - record: dense_index
    invariant: dense.btree_record_size
    scope: aggregate_object
    evidence: fixture
    matches:
      - "dense link v2 B-tree record size is zero"
