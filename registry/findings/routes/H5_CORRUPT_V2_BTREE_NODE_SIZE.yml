# Message routing for H5_CORRUPT_V2_BTREE_NODE_SIZE.
# Authoritative source; the loader expands each matches list and applies
# longest-substring-first precedence.
schema_version: 1
finding: H5_CORRUPT_V2_BTREE_NODE_SIZE

routes:
  - record: heap_structures
    invariant: heap.btree_node_size
    scope: aggregate_object
    evidence: curated
    matches:
      - "dense attribute v2 B-tree node cannot hold one record"
      - "dense attribute creation-order v2 B-tree node cannot hold one record"
      - "dense attribute creation-order v2 B-tree used node bytes exceed"
      # The two hand-written NAME-index leaf readers gained the same
      # capacity check the shared engine and the corder walker already had
      # (corpus seed 63). No substring clash with the corder rules
      # above: those carry "creation-order" between the role and "v2".
      - "dense attribute v2 B-tree used node bytes exceed"
  - record: dense_index
    invariant: dense.btree_node_size
    scope: aggregate_object
    evidence: curated
    matches:
      - "dense link creation-order v2 B-tree node cannot hold one record"
      - "dense link creation-order v2 B-tree used node bytes exceed"
      - "dense link v2 B-tree used node bytes exceed"
      - "dense link v2 B-tree node cannot hold one record"
  - record: shared_messages_legacy
    invariant: sohm.btree_node_size
    scope: aggregate_object
    evidence: curated
    matches:
      - "SOHM v2 B-tree node cannot hold one record"
      - "SOHM v2 B-tree contents exceed the declared node size"
  - record: chunk_index
    invariant: chunk.btree_node_size
    scope: aggregate_object
    evidence: curated
    matches:
      - "chunk index v2 B-tree node cannot hold one record"
      - "v2 B-tree chunk node contents exceed the declared node size"
