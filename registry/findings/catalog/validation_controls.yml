# Finding catalog shard for the validation_controls record family.
# Authoritative source; routing for ambiguous codes lives in ../routes/.
schema_version: 1
record: validation_controls

findings:

  H5_CORRUPT_BASE_ADDRESS_MISMATCH:
    severity: corrupt
    scope: local_decode
    invariant: control.base_address
    record: validation_controls
    versions: [1, 2]
    shared: false
    edge_type: file_address
    emitted_in: [h5policy/pickles/h5_validate.pk]
    message: base address is inconsistent with file geometry

  H5_CORRUPT_FSM_SECTION_COUNT:
    severity: corrupt
    scope: local_decode
    invariant: control.fsm_section_count
    record: validation_controls
    versions: [1, 2]
    shared: false
    edge_type: free_space
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: free-space manager section count is invalid

  H5_CORRUPT_FSM_SECTION_TYPE:
    severity: corrupt
    scope: local_decode
    invariant: control.fsm_section_type
    record: validation_controls
    versions: [1, 2]
    shared: false
    edge_type: free_space
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: free-space manager section type is invalid

  H5_RESOURCE_LINK_TRAVERSAL_DEPTH:
    severity: resource
    scope: resource
    invariant: control.link_depth
    record: validation_controls
    versions: [1, 2]
    shared: false
    edge_type: link
    emitted_in: [h5policy/pickles/h5_walk.pk]
    message: link traversal depth exceeds the validation budget
    # Emitted from more than one walker; the mapping above is a
    # fallback, not an authoritative attribution.  Resolve with the
    # finding message via its route shard.
    ambiguous: true

  H5_UNSUPPORTED_WALK_OPERATION_BUDGET:
    severity: unsupported
    scope: resource
    invariant: control.walk_operations
    record: validation_controls
    versions: [1, 2]
    shared: false
    edge_type: walk
    emitted_in: [h5policy/pickles/h5_policy.pk]
    message: walk operation budget was exhausted

  H5_INTERNAL_INVALID_PROFILE:
    severity: internal
    scope: policy
    invariant: control.profile_valid
    record: validation_controls
    versions: [1, 2]
    shared: false
    edge_type: profile
    emitted_in: [h5policy/pickles/h5_policy.pk]
    message: validation profile is invalid
    # Emitted from more than one walker; the mapping above is a
    # fallback, not an authoritative attribution.  Resolve with the
    # finding message via its route shard.
    ambiguous: true

  H5_ADVISORY_METADATA_RATIO:
    severity: advisory
    scope: resource
    invariant: control.metadata_ratio_advisory
    record: validation_controls
    versions: [1, 2]
    shared: false
    edge_type: metadata
    emitted_in: [h5policy/pickles/h5_walk.pk]
    message: metadata-to-data ratio exceeds the advisory threshold

  H5_UNSUPPORTED_PICKLE_COVERAGE_GAP:
    severity: unsupported
    scope: policy
    invariant: control.coverage_gap
    record: validation_controls
    versions: [1, 2]
    shared: true
    edge_type: coverage
    emitted_in: [h5policy/pickles/h5_validate.pk]
    message: validation reached a recognized coverage gap
    # Emitted from more than one walker; the mapping above is a
    # fallback, not an authoritative attribution.  Resolve with the
    # finding message via its route shard.
    ambiguous: true

  # Superblock-extension and free-space validation controls.

  H5_CORRUPT_BTREEK_MESSAGE_SIZE:
    severity: corrupt
    scope: local_decode
    invariant: control.btree_k_encoding
    record: validation_controls
    versions: [0]
    shared: false
    edge_type: superblock_extension
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: V1 B-tree K-values message has the wrong size

  H5_CORRUPT_BTREEK_PLACEMENT:
    severity: corrupt
    scope: aggregate_object
    invariant: control.btree_k_placement
    record: validation_controls
    versions: [0]
    shared: false
    edge_type: superblock_extension
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: V1 B-tree K-values message is outside the superblock extension

  H5_CORRUPT_BTREEK_VALUE:
    severity: corrupt
    scope: local_decode
    invariant: control.btree_k_encoding
    record: validation_controls
    versions: [0]
    shared: false
    edge_type: superblock_extension
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: V1 B-tree K value is invalid

  H5_CORRUPT_UNSUPPORTED_BTREEK_VERSION:
    severity: corrupt
    scope: local_decode
    invariant: control.btree_k_encoding
    record: validation_controls
    versions: [0]
    shared: false
    edge_type: superblock_extension
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: V1 B-tree K-values message version is unsupported

  H5_CORRUPT_DRVINFO_LENGTH:
    severity: corrupt
    scope: local_decode
    invariant: control.driver_info_encoding
    record: validation_controls
    versions: [0]
    shared: false
    edge_type: superblock_extension
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: driver-info body length is invalid

  H5_CORRUPT_DRVINFO_MESSAGE_TOO_SMALL:
    severity: corrupt
    scope: local_decode
    invariant: control.driver_info_encoding
    record: validation_controls
    versions: [0]
    shared: false
    edge_type: superblock_extension
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: driver-info message is too small

  H5_CORRUPT_DRVINFO_PLACEMENT:
    severity: corrupt
    scope: aggregate_object
    invariant: control.driver_info_placement
    record: validation_controls
    versions: [0]
    shared: false
    edge_type: superblock_extension
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: driver-info message is outside the superblock extension

  H5_CORRUPT_UNSUPPORTED_DRVINFO_VERSION:
    severity: corrupt
    scope: local_decode
    invariant: control.driver_info_encoding
    record: validation_controls
    versions: [0]
    shared: false
    edge_type: superblock_extension
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: driver-info message version is unsupported

  H5_CORRUPT_FREE_SPACE_PAGE_SIZE:
    severity: corrupt
    scope: local_decode
    invariant: control.free_space_page_size
    record: validation_controls
    versions: [1]
    shared: false
    edge_type: free_space
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: free-space page size is invalid

  H5_CORRUPT_FSINFO_MESSAGE_TOO_SMALL:
    severity: corrupt
    scope: local_decode
    invariant: control.fsinfo_complete
    record: validation_controls
    versions: [0, 1]
    shared: false
    edge_type: free_space
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: file-space info message is too small

  H5_CORRUPT_UNSUPPORTED_FSINFO_VERSION:
    severity: corrupt
    scope: local_decode
    invariant: control.fsinfo_version_supported
    record: validation_controls
    versions: [0, 1]
    shared: false
    edge_type: free_space
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: file-space info message version is unsupported

  H5_CORRUPT_FSM_HEADER_CYCLE:
    severity: corrupt
    scope: reference_graph
    invariant: control.fsm_acyclic
    record: validation_controls
    versions: [0]
    shared: false
    edge_type: free_space
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: free-space manager header is referenced twice

  H5_CORRUPT_FSM_SECTION_LIST_CYCLE:
    severity: corrupt
    scope: reference_graph
    invariant: control.fsm_acyclic
    record: validation_controls
    versions: [0]
    shared: false
    edge_type: free_space
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: free-space section list is referenced twice

  H5_CORRUPT_FSM_SECTION_LIST_HEADER:
    severity: corrupt
    scope: reference_graph
    invariant: control.fsm_section_list_header
    record: validation_controls
    versions: [0]
    shared: false
    edge_type: free_space
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: free-space section-list header is inconsistent

  H5_CORRUPT_FSM_SECTION_LIST_SIZE:
    severity: corrupt
    scope: aggregate_object
    invariant: control.fsm_section_list_size
    record: validation_controls
    versions: [0]
    shared: false
    edge_type: free_space
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: free-space section-list size is inconsistent

  H5_CORRUPT_FSM_SECTION_OVERRUN:
    severity: corrupt
    scope: aggregate_object
    invariant: control.fsm_section_bounded
    record: validation_controls
    versions: [0]
    shared: false
    edge_type: free_space
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: free-space section record overruns its list

  H5_CORRUPT_UNSUPPORTED_FSM_VERSION:
    severity: corrupt
    scope: local_decode
    invariant: control.fsm_version_supported
    record: validation_controls
    versions: [0]
    shared: false
    edge_type: free_space
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: free-space manager version is unsupported

  H5_CORRUPT_SUPERBLOCK_K_VALUE:
    severity: corrupt
    scope: local_decode
    invariant: control.superblock_k_values
    record: validation_controls
    versions: [0, 1]
    shared: false
    edge_type: superblock
    emitted_in: [h5policy/pickles/h5_validate.pk]
    message: superblock K value is invalid

  H5_CORRUPT_SUPERBLOCK_RESERVED:
    severity: corrupt
    scope: local_decode
    invariant: control.superblock_reserved_zero
    record: validation_controls
    versions: [0, 1, 2, 3]
    shared: false
    edge_type: superblock
    emitted_in: [h5policy/pickles/h5_validate.pk]
    message: superblock reserved field is nonzero

  H5_CORRUPT_SUPERBLOCK_STATUS_FLAGS:
    severity: corrupt
    scope: local_decode
    invariant: control.superblock_status_flags
    record: validation_controls
    versions: [2, 3]
    shared: false
    edge_type: superblock
    emitted_in: [h5policy/pickles/h5_validate.pk]
    message: superblock status flags are invalid

  H5_CORRUPT_UNSUPPORTED_SUPERBLOCK_FIELD_VERSION:
    severity: corrupt
    scope: local_decode
    invariant: control.superblock_field_version
    record: validation_controls
    versions: [0, 1]
    shared: false
    edge_type: superblock
    emitted_in: [h5policy/pickles/h5_validate.pk]
    message: superblock field version is unsupported

  H5_POLICY_FINDINGS_TRUNCATED:
    severity: warning
    scope: resource
    invariant: control.finding_limit
    record: validation_controls
    versions: [1, 2]
    shared: false
    edge_type: finding_report
    emitted_in: [h5policy/pickles/h5_findings.pk]
    message: finding limit reached; additional findings suppressed

  H5_RESOURCE_ATTRIBUTE_COUNT:
    severity: resource
    scope: resource
    invariant: control.attribute_count
    record: validation_controls
    versions: [1, 2]
    shared: false
    edge_type: attribute
    emitted_in: [h5policy/pickles/h5_messages.pk]
    message: attribute count exceeds the validation budget

  H5_RESOURCE_OBJECT_COUNT:
    severity: resource
    scope: resource
    invariant: control.object_count
    record: validation_controls
    versions: [1, 2]
    shared: false
    edge_type: object_header
    emitted_in: [h5policy/pickles/h5_walk.pk]
    message: object count exceeds the validation budget

  H5_UNSUPPORTED_WALK_TIME_BUDGET:
    severity: unsupported
    scope: resource
    invariant: control.walk_time
    record: validation_controls
    versions: [1, 2]
    shared: false
    edge_type: walk
    emitted_in: [h5policy/pickles/h5_policy.pk]
    message: in-process walk time budget was exhausted

  H5_UNSUPPORTED_WALK_TIMEOUT:
    severity: unsupported
    scope: resource
    invariant: control.wall_timeout
    record: validation_controls
    versions: [1, 2]
    shared: false
    edge_type: walk
    emitted_in: [h5policy/tools/h5policy]
    message: wrapper wall-clock timeout was exhausted

  H5_INTERNAL_ERROR:
    severity: internal
    scope: policy
    invariant: control.internal_error
    record: validation_controls
    versions: [1, 2]
    shared: false
    edge_type: validator
    emitted_in: [h5policy/pickles/h5_policy.pk]
    message: validator raised an unanticipated internal exception
