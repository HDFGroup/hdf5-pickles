# Finding catalog shard for the virtual_dataset record family.
# Authoritative source; routing for ambiguous codes lives in ../routes/.
schema_version: 1
record: virtual_dataset

findings:

  H5_POLICY_VDS:
    severity: policy
    scope: policy
    invariant: vds.no_source_open
    record: virtual_dataset
    versions: [4]
    shared: false
    edge_type: vds_source
    emitted_in: [h5policy/pickles/h5_layout.pk]
    message: virtual dataset source access denied by the active profile

  H5_ADVISORY_VDS_ABSOLUTE_SOURCE:
    severity: warning
    scope: policy
    invariant: vds.source_path_classified
    record: virtual_dataset
    versions: [4]
    shared: false
    edge_type: vds_source
    emitted_in: [h5policy/pickles/h5_vds.pk]
    message: VDS source decoded as an absolute path

  H5_ADVISORY_VDS_PARENT_SOURCE:
    severity: warning
    scope: policy
    invariant: vds.source_path_classified
    record: virtual_dataset
    versions: [4]
    shared: false
    edge_type: vds_source
    emitted_in: [h5policy/pickles/h5_vds.pk]
    message: VDS source decoded as a parent-relative path

  H5_ADVISORY_VDS_RELATIVE_SOURCE:
    severity: warning
    scope: policy
    invariant: vds.source_path_classified
    record: virtual_dataset
    versions: [4]
    shared: false
    edge_type: vds_source
    emitted_in: [h5policy/pickles/h5_vds.pk]
    message: VDS source decoded as a relative path

  H5_CORRUPT_VDS_LAYOUT_TOO_SMALL:
    severity: corrupt
    scope: record_local
    invariant: vds.layout_complete
    record: virtual_dataset
    versions: [4]
    shared: false
    edge_type: vds_source
    emitted_in: [h5policy/pickles/h5_vds.pk]
    message: VDS layout message is too small

  H5_CORRUPT_VDS_MAPPING_HEADER:
    severity: corrupt
    scope: record_local
    invariant: vds.mapping_header_complete
    record: virtual_dataset
    versions: [4]
    shared: false
    edge_type: vds_source
    emitted_in: [h5policy/pickles/h5_vds.pk]
    message: VDS mapping header is truncated

  H5_CORRUPT_VDS_MAPPING_FLAGS:
    severity: corrupt
    scope: record_local
    invariant: vds.mapping_flags_valid
    record: virtual_dataset
    versions: [4]
    shared: false
    edge_type: vds_source
    emitted_in: [h5policy/pickles/h5_vds.pk]
    message: VDS mapping flags are invalid

  H5_CORRUPT_VDS_MAPPING_TRAILING_BYTES:
    severity: corrupt
    scope: record_local
    invariant: vds.no_mapping_trailing_bytes
    record: virtual_dataset
    versions: [4]
    shared: false
    edge_type: vds_source
    emitted_in: [h5policy/pickles/h5_vds.pk]
    message: VDS mapping has unexpected trailing bytes

  H5_CORRUPT_VDS_SELECTION:
    severity: corrupt
    scope: record_local
    invariant: vds.selection_valid
    record: virtual_dataset
    versions: [4]
    shared: false
    edge_type: vds_source
    emitted_in: [h5policy/pickles/h5_vds.pk]
    message: VDS dataspace selection is invalid

  H5_CORRUPT_VDS_SOURCE_NAME:
    severity: corrupt
    scope: local_decode
    invariant: vds.source_name_bounded
    record: virtual_dataset
    versions: [4]
    shared: false
    edge_type: vds_source
    emitted_in: [h5policy/pickles/h5_vds.pk]
    message: VDS source name string is unbounded/unterminated

  H5_CORRUPT_VDS_HEAP_OBJECT_MISSING:
    severity: corrupt
    scope: reference_graph
    invariant: vds.heap_object_present
    record: virtual_dataset
    versions: [4]
    shared: false
    edge_type: vds_source
    emitted_in: [h5policy/pickles/h5_vds.pk]
    message: VDS global-heap object is missing

  # VDS global-heap records and global length representability.

  H5_CORRUPT_GLOBAL_HEAP_HEADER:
    severity: corrupt
    scope: reference_graph
    invariant: vds.global_heap_header
    record: virtual_dataset
    versions: [4]
    shared: false
    edge_type: vds_source
    emitted_in: [h5policy/pickles/h5_vds.pk]
    message: VDS global-heap header is invalid

  H5_CORRUPT_GLOBAL_HEAP_OBJECT:
    severity: corrupt
    scope: reference_graph
    invariant: vds.global_heap_object
    record: virtual_dataset
    versions: [4]
    shared: false
    edge_type: vds_source
    emitted_in: [h5policy/pickles/h5_vds.pk]
    message: VDS global-heap object is invalid
