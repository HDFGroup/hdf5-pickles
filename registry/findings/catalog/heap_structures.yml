# Finding catalog shard for the heap_structures record family.
# Authoritative source; routing for ambiguous codes lives in ../routes/.
schema_version: 1
record: heap_structures

findings:

  H5_CORRUPT_HEAP_OFFSET_OUT_OF_BOUNDS:
    severity: corrupt
    scope: aggregate_object
    invariant: heap.offset_bounds
    record: heap_structures
    versions: [1, 2]
    shared: false
    edge_type: heap
    emitted_in: [h5policy/pickles/h5_group.pk]
    message: heap offset lies outside the data segment

  H5_CORRUPT_LOCAL_HEAP_FREE_LIST:
    severity: corrupt
    scope: aggregate_object
    invariant: heap.free_list_valid
    record: heap_structures
    versions: [1]
    shared: false
    edge_type: heap
    emitted_in: [h5policy/pickles/h5_group.pk]
    message: local heap free-list entry is invalid

  H5_CORRUPT_FRACTAL_HEAP_ID:
    severity: corrupt
    scope: aggregate_object
    invariant: heap.fractal_id_valid
    record: heap_structures
    # Emitted from more than one walker; the mapping above is a
    # fallback, not an authoritative attribution.  Resolve with the
    # finding message via its route shard.
    ambiguous: true
    versions: [1]
    shared: false
    edge_type: heap
    emitted_in: [h5policy/pickles/h5_dense_links.pk, h5policy/pickles/h5_dense_attrs.pk]
    message: fractal heap ID is invalid

  H5_RESOURCE_GLOBAL_HEAP_INFINITE_LOOP:
    severity: resource
    scope: resource
    invariant: heap.global_progress
    record: heap_structures
    versions: [1]
    shared: false
    edge_type: heap
    emitted_in: [h5policy/pickles/h5_validate.pk]
    message: global heap traversal exceeded its progress budget

  # Dense attribute/link indexes and their fractal heaps.

  H5_CORRUPT_ATTRIBUTE_INFO_ADDRESS_PAIR:
    severity: corrupt
    scope: aggregate_object
    invariant: heap.attribute_info_addresses
    record: heap_structures
    versions: [0]
    shared: false
    edge_type: attribute_index
    emitted_in: [h5policy/pickles/h5_dense_attrs.pk]
    message: attribute-info heap and name-index addresses are inconsistent

  H5_CORRUPT_ATTRIBUTE_INFO_CORDER_ADDRESS:
    severity: corrupt
    scope: aggregate_object
    invariant: heap.attribute_info_addresses
    record: heap_structures
    versions: [0]
    shared: false
    edge_type: attribute_index
    emitted_in: [h5policy/pickles/h5_dense_attrs.pk]
    message: attribute-info creation-order address is inconsistent

  H5_CORRUPT_ATTRIBUTE_INFO_FLAGS:
    severity: corrupt
    scope: local_decode
    invariant: heap.attribute_info_flags
    record: heap_structures
    versions: [0]
    shared: false
    edge_type: attribute_index
    emitted_in: [h5policy/pickles/h5_dense_attrs.pk]
    message: attribute-info flags are invalid

  H5_CORRUPT_ATTRIBUTE_INFO_MESSAGE_TOO_SMALL:
    severity: corrupt
    scope: local_decode
    invariant: heap.attribute_info_complete
    record: heap_structures
    versions: [0]
    shared: false
    edge_type: attribute_index
    emitted_in: [h5policy/pickles/h5_dense_attrs.pk]
    message: attribute-info message is too small

  H5_CORRUPT_ATTRIBUTE_INFO_TRAILING_BYTES:
    severity: corrupt
    scope: local_decode
    invariant: heap.attribute_info_no_trailing_bytes
    record: heap_structures
    versions: [0]
    shared: false
    edge_type: attribute_index
    emitted_in: [h5policy/pickles/h5_dense_attrs.pk]
    message: attribute-info message has trailing bytes

  H5_CORRUPT_UNSUPPORTED_ATTRIBUTE_INFO_VERSION:
    severity: corrupt
    scope: local_decode
    invariant: heap.attribute_info_version
    record: heap_structures
    versions: [0]
    shared: false
    edge_type: attribute_index
    emitted_in: [h5policy/pickles/h5_dense_attrs.pk]
    message: attribute-info message version is unsupported

  H5_CORRUPT_FRACTAL_HEAP_ID_WIDTH:
    severity: corrupt
    scope: aggregate_object
    invariant: heap.fractal_id_width
    record: heap_structures
    ambiguous: true
    versions: [0]
    shared: false
    edge_type: heap
    emitted_in: [h5policy/pickles/h5_dense_attrs.pk, h5policy/pickles/h5_dense_links.pk]
    message: fractal-heap ID widths do not fit their owning index record

  H5_CORRUPT_UNSUPPORTED_FRACTAL_HEAP_VERSION:
    severity: corrupt
    scope: local_decode
    invariant: heap.fractal_version_supported
    record: heap_structures
    ambiguous: true
    versions: [0]
    shared: true
    edge_type: heap
    emitted_in: [h5policy/pickles/h5_dense_attrs.pk, h5policy/pickles/h5_dense_links.pk, h5policy/pickles/h5_sohm.pk]
    message: fractal-heap structure version is unsupported

  H5_CORRUPT_LOCAL_HEAP_RESERVED:
    severity: corrupt
    scope: local_decode
    invariant: heap.local_reserved_zero
    record: heap_structures
    versions: [0]
    shared: false
    edge_type: heap
    emitted_in: [h5policy/pickles/h5_group.pk]
    message: local-heap reserved fields are nonzero

  H5_CORRUPT_UNSUPPORTED_LOCAL_HEAP_VERSION:
    severity: corrupt
    scope: local_decode
    invariant: heap.local_version_supported
    record: heap_structures
    versions: [0]
    shared: false
    edge_type: heap
    emitted_in: [h5policy/pickles/h5_group.pk]
    message: local-heap version is unsupported
