# Finding catalog shard for the dense_index record family.
# Authoritative source; routing for ambiguous codes lives in ../routes/.
schema_version: 1
record: dense_index

findings:

  H5_CORRUPT_DENSE_INDEX_COUNT_MISMATCH:
    severity: corrupt
    scope: aggregate_object
    invariant: dense.count_match
    record: dense_index
    versions: [1]
    shared: false
    edge_type: dense_index
    emitted_in: [h5policy/pickles/h5_dense_links.pk]
    message: dense index count does not match its entries

  H5_CORRUPT_DENSE_NAME_INDEX_DUPLICATE_ID:
    severity: corrupt
    scope: aggregate_object
    invariant: dense.unique_name_ids
    record: dense_index
    versions: [1]
    shared: false
    edge_type: dense_index
    emitted_in: [h5policy/pickles/h5_dense_links.pk]
    message: dense name index contains a duplicate ID

  H5_CORRUPT_LINK_NAME_EMPTY:
    severity: corrupt
    scope: local_decode
    invariant: dense.link_name_nonempty
    record: dense_index
    # Emitted from more than one walker; the mapping above is a
    # fallback, not an authoritative attribution.  Resolve with the
    # finding message via its route shard.
    ambiguous: true
    versions: [1, 2]
    shared: false
    edge_type: link
    emitted_in: [h5policy/pickles/h5_group.pk, h5policy/pickles/h5_dense_links.pk]
    message: link name is empty

  H5_CORRUPT_LINK_NAME_CHARSET:
    severity: corrupt
    scope: local_decode
    invariant: dense.link_name_charset
    record: dense_index
    versions: [1, 2]
    shared: false
    edge_type: link
    emitted_in: [h5policy/pickles/h5_dense_links.pk]
    message: link name charset is invalid

  H5_CORRUPT_DENSE_CORDER_INDEX_DUPLICATE_ID:
    severity: corrupt
    scope: aggregate_object
    invariant: dense.corder_index_closure
    record: dense_index
    ambiguous: true
    versions: [0]
    shared: false
    edge_type: dense_index
    emitted_in: [h5policy/pickles/h5_dense_links.pk]
    message: dense creation-order index contains a duplicate heap ID

  H5_CORRUPT_DENSE_CORDER_INDEX_MISSING_ID:
    severity: corrupt
    scope: aggregate_object
    invariant: dense.corder_index_closure
    record: dense_index
    ambiguous: true
    versions: [0]
    shared: false
    edge_type: dense_index
    emitted_in: [h5policy/pickles/h5_dense_links.pk]
    message: dense creation-order index is missing a name-index heap ID

  H5_CORRUPT_DENSE_CORDER_INDEX_UNKNOWN_ID:
    severity: corrupt
    scope: aggregate_object
    invariant: dense.corder_index_closure
    record: dense_index
    ambiguous: true
    versions: [0]
    shared: false
    edge_type: dense_index
    emitted_in: [h5policy/pickles/h5_dense_links.pk]
    message: dense creation-order index refers to an unknown heap ID

  H5_CORRUPT_DENSE_CORDER_MISMATCH:
    severity: corrupt
    scope: aggregate_object
    invariant: dense.corder_index_closure
    record: dense_index
    ambiguous: true
    versions: [0]
    shared: false
    edge_type: dense_index
    emitted_in: [h5policy/pickles/h5_dense_links.pk]
    message: dense creation-order record disagrees with its link message

  H5_CORRUPT_LINK_INFO_ADDRESS_PAIR:
    severity: corrupt
    scope: aggregate_object
    invariant: dense.link_info_addresses
    record: dense_index
    versions: [0]
    shared: false
    edge_type: dense_index
    emitted_in: [h5policy/pickles/h5_dense_links.pk]
    message: link-info heap and name-index addresses are inconsistent

  H5_CORRUPT_LINK_INFO_CORDER_ADDRESS:
    severity: corrupt
    scope: aggregate_object
    invariant: dense.link_info_addresses
    record: dense_index
    versions: [0]
    shared: false
    edge_type: dense_index
    emitted_in: [h5policy/pickles/h5_dense_links.pk]
    message: link-info creation-order address is inconsistent

  H5_CORRUPT_V2_BTREE_CHILD_POINTER:
    severity: corrupt
    scope: reference_graph
    invariant: dense.btree_child_pointer
    record: dense_index
    ambiguous: true
    versions: [0]
    shared: false
    edge_type: dense_index
    emitted_in: [h5policy/pickles/h5_dense_links.pk]
    message: dense-index V2 B-tree child pointer is invalid

  H5_CORRUPT_V2_BTREE_CYCLE:
    severity: corrupt
    scope: reference_graph
    invariant: dense.btree_no_cycle
    record: dense_index
    ambiguous: true
    versions: [0]
    shared: false
    edge_type: dense_index
    emitted_in: [h5policy/pickles/h5_dense_links.pk]
    message: dense-index V2 B-tree contains a cycle

  H5_CORRUPT_V2_BTREE_DUPLICATE_KEY:
    severity: corrupt
    scope: aggregate_object
    invariant: dense.btree_unique_keys
    record: dense_index
    ambiguous: true
    versions: [0]
    shared: false
    edge_type: dense_index
    emitted_in: [h5policy/pickles/h5_dense_links.pk]
    message: dense-index V2 B-tree contains a duplicate key

  H5_CORRUPT_V2_BTREE_RECORD_ORDER:
    severity: corrupt
    scope: aggregate_object
    invariant: dense.btree_record_order
    record: dense_index
    ambiguous: true
    versions: [0]
    shared: false
    edge_type: dense_index
    emitted_in: [h5policy/pickles/h5_dense_links.pk]
    message: dense-index V2 B-tree records are out of order

  H5_CORRUPT_V2_BTREE_RECORD_TYPE:
    severity: corrupt
    scope: aggregate_object
    invariant: dense.btree_record_type
    record: dense_index
    ambiguous: true
    versions: [0]
    shared: false
    edge_type: dense_index
    emitted_in: [h5policy/pickles/h5_dense_links.pk]
    message: dense-index V2 B-tree record type is invalid

  H5_CORRUPT_V2_BTREE_ROOT:
    severity: corrupt
    scope: reference_graph
    invariant: dense.btree_root
    record: dense_index
    ambiguous: true
    versions: [0]
    shared: false
    edge_type: dense_index
    emitted_in: [h5policy/pickles/h5_dense_links.pk]
    message: dense-index V2 B-tree root is invalid

  H5_CORRUPT_V2_BTREE_TOTAL_COUNT:
    severity: corrupt
    scope: aggregate_object
    invariant: dense.btree_total_count
    record: dense_index
    ambiguous: true
    versions: [0]
    shared: false
    edge_type: dense_index
    emitted_in: [h5policy/pickles/h5_dense_links.pk]
    message: dense-index V2 B-tree total record count is inconsistent
