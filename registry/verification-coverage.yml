# GENERATED by `h5cve verification` -- do not hand-edit.
#
# Strategy-doc §12 per record family: which of the eleven verification
# requirements are demonstrably met by the artifacts in this tree.
#
#   met           mechanically demonstrated, evidence listed
#   partial       demonstrated for some of the requirement, not all
#   absent        mechanically demonstrated to be MISSING
#   not_assessed  not determinable from artifacts; needs classification
#
# `not_assessed` is NOT a soft `met`. Requirements 2-4 default to that
# state; reviewed annotations can establish partial or complete
# coverage without inferring it from finding-code spelling.
schema_version: 1
generated_by: h5cve verification
requirements:
- truncation_every_byte_boundary
- count_and_extent_boundaries
- integer_overflow_and_allocation_budget
- deep_nesting_and_non_progress
- reference_semantics_cases
- stable_finding_and_offset_assertions
- no_activation_on_validation_failure
- differential_vs_legacy_on_valid_inputs
- in_memory_fuzz_targets
- full_file_cve_and_ossfuzz
- lazy_validation_performance
records:
  object_header_continuation:
    truncation_every_byte_boundary:
      status: met
      note: h5policy-truncate walks every byte boundary of a seed contracted to this
        family, with no escape, internal error or hang.
      evidence:
      - h5policy/tests/valid/continuation_chunks.h5
    count_and_extent_boundaries:
      status: partial
      note: 'Reviewed fixture/recipe annotations cover: n, n_plus_one, out_of_file,
        zero. Missing: max, n_minus_1.'
      evidence:
      - h5policy/tests/expected/integration-object_header_chunks.yml
      - h5policy/tools/h5mutate:cont_out_of_file
      - h5policy/tools/h5mutate:cont_zero_size
    integer_overflow_and_allocation_budget:
      status: not_assessed
      note: Budget coverage is partly visible via reject_resource fixtures, but integer-overflow
        coverage cannot be separated without classifying fixtures. Deliberately not
        inferred from finding-code spelling.
    deep_nesting_and_non_progress:
      status: partial
      note: 'Reviewed fixture/recipe annotations cover: non_progress. Missing: deep_nesting.'
      evidence:
      - h5policy/tools/h5mutate:cont_alias_prior_chunk
    reference_semantics_cases:
      status: partial
      note: 'Reviewed fixture/recipe annotations cover: duplicate_reference, overlap,
        self_reference. Not covered: cycle.'
      evidence:
      - h5policy/tests/expected/malformed-continuation_overlaps_source.yml
      - h5policy/tools/h5mutate:cont_alias_prior_chunk
      - h5policy/tools/h5mutate:cont_overlap_source_end
      - h5policy/tools/h5mutate:cont_overlap_source_inside
      - h5policy/tools/h5mutate:cont_overlap_source_start
    stable_finding_and_offset_assertions:
      status: met
      note: Fixtures assert both finding codes and evidence locations.
      evidence:
      - malformed-continuation_overlaps_source.yml
    no_activation_on_validation_failure:
      status: met
      note: Exact-build probe drives this family with external_open, plugin_load,
        write, network and crash forbidden; no rejecting fixture triggers one.
    differential_vs_legacy_on_valid_inputs:
      status: met
      note: h5policy-diff cross-checks every corpus file against libhdf5; this family
        has an accepting fixture in that set.
      evidence:
      - valid-continuation_chunks.yml
    in_memory_fuzz_targets:
      status: met
      note: 6 built typed mutation recipe(s) (2 planned).
      evidence:
      - h5policy/tests/valid/continuation_chunks.h5
    full_file_cve_and_ossfuzz:
      status: absent
      note: No full-file cve/ fixture, and no OSS-Fuzz integration.
    lazy_validation_performance:
      status: partial
      note: h5policy-lazy shows validation cost flat while raw data grows 3031x, with
        a sensitivity control proving the counters respond to real metadata growth.
        Measured on the oracle as a whole, not per family, so it does not yet discharge
        this requirement family by family.
  external_file_list:
    truncation_every_byte_boundary:
      status: met
      note: h5policy-truncate walks every byte boundary of a seed contracted to this
        family, with no escape, internal error or hang.
      evidence:
      - h5policy/tests/policy/external_storage.h5
      - h5policy/tests/policy/external_storage_parent_unlimited.h5
    count_and_extent_boundaries:
      status: not_assessed
      note: Requires classifying each fixture as a zero/max/N-1/N/N+1 case. Not derivable
        from the corpus as it stands.
    integer_overflow_and_allocation_budget:
      status: not_assessed
      note: Budget coverage is partly visible via reject_resource fixtures, but integer-overflow
        coverage cannot be separated without classifying fixtures. Deliberately not
        inferred from finding-code spelling.
    deep_nesting_and_non_progress:
      status: not_assessed
      note: 'Same: depth and non-progress fixtures exist for some families but are
        not distinguishable from the corpus without classification.'
    reference_semantics_cases:
      status: not_assessed
      note: No invariant in this family maps to a reviewed self-reference / overlap
        / duplicate-reference / cycle category. That may be correct for a non-reference-bearing
        family, or the mapping may be incomplete.
    stable_finding_and_offset_assertions:
      status: met
      note: Fixtures assert both finding codes and evidence locations.
      evidence:
      - malformed-bad_efl_slot_count.yml
    no_activation_on_validation_failure:
      status: met
      note: Exact-build probe drives this family with external_open, plugin_load,
        write, network and crash forbidden; no rejecting fixture triggers one.
    differential_vs_legacy_on_valid_inputs:
      status: met
      note: h5policy-diff cross-checks every corpus file against libhdf5; this family
        has an accepting fixture in that set.
      evidence:
      - policy-external_storage-trusted_fast.yml
      - policy-external_storage_parent_unlimited.yml
    in_memory_fuzz_targets:
      status: absent
      note: No fuzz_targets declared. h5policy-fuzz mutates whole-file seeds generically;
        this requires a dedicated target for the family.
    full_file_cve_and_ossfuzz:
      status: absent
      note: No full-file cve/ fixture, and no OSS-Fuzz integration.
    lazy_validation_performance:
      status: partial
      note: h5policy-lazy shows validation cost flat while raw data grows 3031x, with
        a sensitivity control proving the counters respond to real metadata growth.
        Measured on the oracle as a whole, not per family, so it does not yet discharge
        this requirement family by family.
  external_link:
    truncation_every_byte_boundary:
      status: met
      note: h5policy-truncate walks every byte boundary of a seed contracted to this
        family, with no escape, internal error or hang.
      evidence:
      - h5policy/tests/policy/external_link.h5
      - h5policy/tests/policy/external_link_parent.h5
    count_and_extent_boundaries:
      status: not_assessed
      note: Requires classifying each fixture as a zero/max/N-1/N/N+1 case. Not derivable
        from the corpus as it stands.
    integer_overflow_and_allocation_budget:
      status: not_assessed
      note: Budget coverage is partly visible via reject_resource fixtures, but integer-overflow
        coverage cannot be separated without classifying fixtures. Deliberately not
        inferred from finding-code spelling.
    deep_nesting_and_non_progress:
      status: not_assessed
      note: 'Same: depth and non-progress fixtures exist for some families but are
        not distinguishable from the corpus without classification.'
    reference_semantics_cases:
      status: not_assessed
      note: No invariant in this family maps to a reviewed self-reference / overlap
        / duplicate-reference / cycle category. That may be correct for a non-reference-bearing
        family, or the mapping may be incomplete.
    stable_finding_and_offset_assertions:
      status: partial
      note: Finding codes are asserted; no fixture pins an evidence location (expected_finding_evidence_locations).
      evidence:
      - malformed-bad_external_link_info.yml
      - malformed-external_link_unterminated_value.yml
      - policy-external_link-trusted_fast.yml
      - policy-external_link.yml
      - policy-external_link_parent-legacy.yml
    no_activation_on_validation_failure:
      status: partial
      note: The probe forbids activation and the family is exercised, but a rejecting
        fixture still activates, crashes or hangs the build under test.
      evidence:
      - malformed-external_link_unterminated_value.yml
    differential_vs_legacy_on_valid_inputs:
      status: met
      note: h5policy-diff cross-checks every corpus file against libhdf5; this family
        has an accepting fixture in that set.
      evidence:
      - policy-external_link-trusted_fast.yml
      - policy-external_link_parent-legacy.yml
    in_memory_fuzz_targets:
      status: absent
      note: No fuzz_targets declared. h5policy-fuzz mutates whole-file seeds generically;
        this requires a dedicated target for the family.
    full_file_cve_and_ossfuzz:
      status: absent
      note: No full-file cve/ fixture, and no OSS-Fuzz integration.
    lazy_validation_performance:
      status: partial
      note: h5policy-lazy shows validation cost flat while raw data grows 3031x, with
        a sensitivity control proving the counters respond to real metadata growth.
        Measured on the oracle as a whole, not per family, so it does not yet discharge
        this requirement family by family.
  virtual_dataset:
    truncation_every_byte_boundary:
      status: partial
      note: The family's seed exceeds the sweep budget, so boundaries were strided
        rather than exhausted. A sampled sweep is not the every-boundary coverage
        this requires.
      evidence:
      - h5policy/tests/policy/vds_absolute_source.h5
    count_and_extent_boundaries:
      status: not_assessed
      note: Requires classifying each fixture as a zero/max/N-1/N/N+1 case. Not derivable
        from the corpus as it stands.
    integer_overflow_and_allocation_budget:
      status: not_assessed
      note: Budget coverage is partly visible via reject_resource fixtures, but integer-overflow
        coverage cannot be separated without classifying fixtures. Deliberately not
        inferred from finding-code spelling.
    deep_nesting_and_non_progress:
      status: not_assessed
      note: 'Same: depth and non-progress fixtures exist for some families but are
        not distinguishable from the corpus without classification.'
    reference_semantics_cases:
      status: not_assessed
      note: No invariant in this family maps to a reviewed self-reference / overlap
        / duplicate-reference / cycle category. That may be correct for a non-reference-bearing
        family, or the mapping may be incomplete.
    stable_finding_and_offset_assertions:
      status: met
      note: Fixtures assert both finding codes and evidence locations.
      evidence:
      - malformed-bad_vds_gheap_free_unaligned.yml
      - malformed-bad_vds_gheap_size_too_small.yml
    no_activation_on_validation_failure:
      status: partial
      note: The probe forbids activation and the family is exercised, but a rejecting
        fixture still activates, crashes or hangs the build under test.
      evidence:
      - cve-vds_nentries_mult_overflow.yml
      - malformed-bad_vds_gheap_free_zero_length.yml
    differential_vs_legacy_on_valid_inputs:
      status: met
      note: h5policy-diff cross-checks every corpus file against libhdf5; this family
        has an accepting fixture in that set.
      evidence:
      - policy-vds_absolute_source.yml
      - policy-vds_hyperslab.yml
      - policy-vds_selection_v2.yml
    in_memory_fuzz_targets:
      status: absent
      note: No fuzz_targets declared. h5policy-fuzz mutates whole-file seeds generically;
        this requires a dedicated target for the family.
    full_file_cve_and_ossfuzz:
      status: partial
      note: Full-file CVE fixture present; OSS-Fuzz integration does not exist in
        this repository.
      evidence:
      - cve-vds_nentries_mult_overflow.yml
    lazy_validation_performance:
      status: partial
      note: h5policy-lazy shows validation cost flat while raw data grows 3031x, with
        a sensitivity control proving the counters respond to real metadata growth.
        Measured on the oracle as a whole, not per family, so it does not yet discharge
        this requirement family by family.
  datatypes:
    truncation_every_byte_boundary:
      status: met
      note: h5policy-truncate walks every byte boundary of a seed contracted to this
        family, with no escape, internal error or hang.
      evidence:
      - h5policy/tests/valid/empty.h5
      - h5policy/tests/valid/nested_datatypes.h5
    count_and_extent_boundaries:
      status: partial
      note: 'Reviewed fixture/recipe annotations cover: zero. Missing: max, n, n_minus_1,
        n_plus_one.'
      evidence:
      - h5policy/tests/expected/malformed-bad_compound.yml
    integer_overflow_and_allocation_budget:
      status: partial
      note: 'Reviewed fixture/recipe annotations cover: integer_overflow. Missing:
        allocation_budget.'
      evidence:
      - h5policy/tests/expected/malformed-compound_member_array_nelem_overflow.yml
      - h5policy/tests/expected/malformed-compound_member_offset_oob.yml
    deep_nesting_and_non_progress:
      status: partial
      note: 'Reviewed fixture/recipe annotations cover: deep_nesting. Missing: non_progress.'
      evidence:
      - h5policy/tests/expected/cve-dtype_compound_unbounded_recursion.yml
      - h5policy/tests/expected/cve-dtype_unbounded_recursion.yml
    reference_semantics_cases:
      status: not_assessed
      note: No invariant in this family maps to a reviewed self-reference / overlap
        / duplicate-reference / cycle category. That may be correct for a non-reference-bearing
        family, or the mapping may be incomplete.
    stable_finding_and_offset_assertions:
      status: met
      note: Fixtures assert both finding codes and evidence locations.
      evidence:
      - malformed-bad_dtype_duplicate_member_name.yml
      - malformed-bad_dtype_float_normalization.yml
      - malformed-bad_dtype_float_overlap.yml
      - malformed-bad_dtype_opaque_tag_unaligned.yml
    no_activation_on_validation_failure:
      status: met
      note: Exact-build probe drives this family with external_open, plugin_load,
        write, network and crash forbidden; no rejecting fixture triggers one.
    differential_vs_legacy_on_valid_inputs:
      status: met
      note: h5policy-diff cross-checks every corpus file against libhdf5; this family
        has an accepting fixture in that set.
      evidence:
      - valid-empty.yml
      - valid-nested_datatypes.yml
    in_memory_fuzz_targets:
      status: absent
      note: No fuzz_targets declared. h5policy-fuzz mutates whole-file seeds generically;
        this requires a dedicated target for the family.
    full_file_cve_and_ossfuzz:
      status: partial
      note: Full-file CVE fixture present; OSS-Fuzz integration does not exist in
        this repository.
      evidence:
      - cve-dtype_compound_unbounded_recursion.yml
      - cve-dtype_unbounded_recursion.yml
    lazy_validation_performance:
      status: partial
      note: h5policy-lazy shows validation cost flat while raw data grows 3031x, with
        a sensitivity control proving the counters respond to real metadata growth.
        Measured on the oracle as a whole, not per family, so it does not yet discharge
        this requirement family by family.
  btree_heap_index:
    truncation_every_byte_boundary:
      status: met
      note: h5policy-truncate walks every byte boundary of a seed contracted to this
        family, with no escape, internal error or hang.
      evidence:
      - h5policy/tests/valid/empty.h5
    count_and_extent_boundaries:
      status: not_assessed
      note: Requires classifying each fixture as a zero/max/N-1/N/N+1 case. Not derivable
        from the corpus as it stands.
    integer_overflow_and_allocation_budget:
      status: not_assessed
      note: Budget coverage is partly visible via reject_resource fixtures, but integer-overflow
        coverage cannot be separated without classifying fixtures. Deliberately not
        inferred from finding-code spelling.
    deep_nesting_and_non_progress:
      status: not_assessed
      note: 'Same: depth and non-progress fixtures exist for some families but are
        not distinguishable from the corpus without classification.'
    reference_semantics_cases:
      status: not_assessed
      note: No invariant in this family maps to a reviewed self-reference / overlap
        / duplicate-reference / cycle category. That may be correct for a non-reference-bearing
        family, or the mapping may be incomplete.
    stable_finding_and_offset_assertions:
      status: met
      note: Fixtures assert both finding codes and evidence locations.
      evidence:
      - malformed-bad_group_btree_child_address.yml
    no_activation_on_validation_failure:
      status: met
      note: Exact-build probe drives this family with external_open, plugin_load,
        write, network and crash forbidden; no rejecting fixture triggers one.
    differential_vs_legacy_on_valid_inputs:
      status: met
      note: h5policy-diff cross-checks every corpus file against libhdf5; this family
        has an accepting fixture in that set.
      evidence:
      - valid-dense_links_deep.yml
      - valid-empty.yml
    in_memory_fuzz_targets:
      status: absent
      note: No fuzz_targets declared. h5policy-fuzz mutates whole-file seeds generically;
        this requires a dedicated target for the family.
    full_file_cve_and_ossfuzz:
      status: absent
      note: No full-file cve/ fixture, and no OSS-Fuzz integration.
    lazy_validation_performance:
      status: partial
      note: h5policy-lazy shows validation cost flat while raw data grows 3031x, with
        a sensitivity control proving the counters respond to real metadata growth.
        Measured on the oracle as a whole, not per family, so it does not yet discharge
        this requirement family by family.
  dataset_layout_filter_fill:
    truncation_every_byte_boundary:
      status: met
      note: h5policy-truncate walks every byte boundary of a seed contracted to this
        family, with no escape, internal error or hang.
      evidence:
      - h5policy/tests/valid/empty.h5
    count_and_extent_boundaries:
      status: partial
      note: 'Reviewed fixture/recipe annotations cover: n, n_plus_one, zero. Missing:
        max, n_minus_1.'
      evidence:
      - h5policy/tests/expected/integration-chunk_count.yml
      - h5policy/tests/expected/integration-filter_count.yml
      - h5policy/tests/expected/malformed-layout_v5_zero_ndims.yml
    integer_overflow_and_allocation_budget:
      status: met
      note: 'Reviewed fixture/recipe annotations cover: allocation_budget, integer_overflow.'
      evidence:
      - h5policy/tests/expected/malformed-bad_dataset_storage_overflow.yml
      - h5policy/tests/expected/malformed-chunk_size_uint32_wrap.yml
      - h5policy/tests/expected/malformed-scaleoffset_size_mismatch.yml
      - h5policy/tests/expected/resource-huge_logical_dataset.yml
    deep_nesting_and_non_progress:
      status: not_assessed
      note: 'Same: depth and non-progress fixtures exist for some families but are
        not distinguishable from the corpus without classification.'
    reference_semantics_cases:
      status: not_assessed
      note: No invariant in this family maps to a reviewed self-reference / overlap
        / duplicate-reference / cycle category. That may be correct for a non-reference-bearing
        family, or the mapping may be incomplete.
    stable_finding_and_offset_assertions:
      status: met
      note: Fixtures assert both finding codes and evidence locations.
      evidence:
      - malformed-bad_fill_v3_flag_conflict.yml
      - malformed-bad_fill_v3_unknown_flags.yml
      - malformed-bad_filter_count_over_format_max.yml
      - malformed-bad_filter_name_length_unaligned.yml
      - malformed-bad_layout_chunk_ndims_one.yml
      - malformed-bad_layout_rank_mismatch.yml
      - malformed-chunk_size_uint32_wrap.yml
      - malformed-scaleoffset_nelmts_mismatch.yml
      - malformed-scaleoffset_size_mismatch.yml
    no_activation_on_validation_failure:
      status: met
      note: Exact-build probe drives this family with external_open, plugin_load,
        write, network and crash forbidden; no rejecting fixture triggers one.
    differential_vs_legacy_on_valid_inputs:
      status: met
      note: h5policy-diff cross-checks every corpus file against libhdf5; this family
        has an accepting fixture in that set.
      evidence:
      - integration-multi_filter-baseline.yml
      - valid-empty.yml
    in_memory_fuzz_targets:
      status: absent
      note: No fuzz_targets declared. h5policy-fuzz mutates whole-file seeds generically;
        this requires a dedicated target for the family.
    full_file_cve_and_ossfuzz:
      status: absent
      note: No full-file cve/ fixture, and no OSS-Fuzz integration.
    lazy_validation_performance:
      status: partial
      note: h5policy-lazy shows validation cost flat while raw data grows 3031x, with
        a sensitivity control proving the counters respond to real metadata growth.
        Measured on the oracle as a whole, not per family, so it does not yet discharge
        this requirement family by family.
  cache_image_dependency_graph:
    truncation_every_byte_boundary:
      status: met
      note: h5policy-truncate walks every byte boundary of a seed contracted to this
        family, with no escape, internal error or hang.
      evidence:
      - h5policy/tests/valid/cache_image.h5
      - h5policy/tests/valid/empty.h5
    count_and_extent_boundaries:
      status: not_assessed
      note: Requires classifying each fixture as a zero/max/N-1/N/N+1 case. Not derivable
        from the corpus as it stands.
    integer_overflow_and_allocation_budget:
      status: not_assessed
      note: Budget coverage is partly visible via reject_resource fixtures, but integer-overflow
        coverage cannot be separated without classifying fixtures. Deliberately not
        inferred from finding-code spelling.
    deep_nesting_and_non_progress:
      status: not_assessed
      note: 'Same: depth and non-progress fixtures exist for some families but are
        not distinguishable from the corpus without classification.'
    reference_semantics_cases:
      status: not_assessed
      note: No invariant in this family maps to a reviewed self-reference / overlap
        / duplicate-reference / cycle category. That may be correct for a non-reference-bearing
        family, or the mapping may be incomplete.
    stable_finding_and_offset_assertions:
      status: met
      note: Fixtures assert both finding codes and evidence locations.
      evidence:
      - cve-cve_2020_10812.yml
    no_activation_on_validation_failure:
      status: met
      note: Exact-build probe drives this family with external_open, plugin_load,
        write, network and crash forbidden; no rejecting fixture triggers one.
    differential_vs_legacy_on_valid_inputs:
      status: met
      note: h5policy-diff cross-checks every corpus file against libhdf5; this family
        has an accepting fixture in that set.
      evidence:
      - valid-cache_image.yml
      - valid-empty.yml
      - valid-userblock_cache_image.yml
    in_memory_fuzz_targets:
      status: absent
      note: No fuzz_targets declared. h5policy-fuzz mutates whole-file seeds generically;
        this requires a dedicated target for the family.
    full_file_cve_and_ossfuzz:
      status: partial
      note: Full-file CVE fixture present; OSS-Fuzz integration does not exist in
        this repository.
      evidence:
      - cve-cve_2020_10812.yml
      - cve-mdci_entry_offby_header.yml
    lazy_validation_performance:
      status: partial
      note: h5policy-lazy shows validation cost flat while raw data grows 3031x, with
        a sensitivity control proving the counters respond to real metadata growth.
        Measured on the oracle as a whole, not per family, so it does not yet discharge
        this requirement family by family.
  dataspace_dimension:
    truncation_every_byte_boundary:
      status: met
      note: h5policy-truncate walks every byte boundary of a seed contracted to this
        family, with no escape, internal error or hang.
      evidence:
      - h5policy/tests/valid/chunk_single.h5
      - h5policy/tests/valid/empty.h5
    count_and_extent_boundaries:
      status: not_assessed
      note: Requires classifying each fixture as a zero/max/N-1/N/N+1 case. Not derivable
        from the corpus as it stands.
    integer_overflow_and_allocation_budget:
      status: not_assessed
      note: Budget coverage is partly visible via reject_resource fixtures, but integer-overflow
        coverage cannot be separated without classifying fixtures. Deliberately not
        inferred from finding-code spelling.
    deep_nesting_and_non_progress:
      status: not_assessed
      note: 'Same: depth and non-progress fixtures exist for some families but are
        not distinguishable from the corpus without classification.'
    reference_semantics_cases:
      status: not_assessed
      note: No invariant in this family maps to a reviewed self-reference / overlap
        / duplicate-reference / cycle category. That may be correct for a non-reference-bearing
        family, or the mapping may be incomplete.
    stable_finding_and_offset_assertions:
      status: met
      note: Fixtures assert both finding codes and evidence locations.
      evidence:
      - malformed-bad_duplicate_dataspace_message.yml
    no_activation_on_validation_failure:
      status: met
      note: Exact-build probe drives this family with external_open, plugin_load,
        write, network and crash forbidden; no rejecting fixture triggers one.
    differential_vs_legacy_on_valid_inputs:
      status: met
      note: h5policy-diff cross-checks every corpus file against libhdf5; this family
        has an accepting fixture in that set.
      evidence:
      - valid-chunk_single.yml
      - valid-empty.yml
    in_memory_fuzz_targets:
      status: absent
      note: No fuzz_targets declared. h5policy-fuzz mutates whole-file seeds generically;
        this requires a dedicated target for the family.
    full_file_cve_and_ossfuzz:
      status: absent
      note: No full-file cve/ fixture, and no OSS-Fuzz integration.
    lazy_validation_performance:
      status: partial
      note: h5policy-lazy shows validation cost flat while raw data grows 3031x, with
        a sensitivity control proving the counters respond to real metadata growth.
        Measured on the oracle as a whole, not per family, so it does not yet discharge
        this requirement family by family.
  address_space_bounds:
    truncation_every_byte_boundary:
      status: partial
      note: The family's seed exceeds the sweep budget, so boundaries were strided
        rather than exhausted. A sampled sweep is not the every-boundary coverage
        this requires.
      evidence:
      - h5policy/tests/valid/userblock_earliest.h5
    count_and_extent_boundaries:
      status: not_assessed
      note: Requires classifying each fixture as a zero/max/N-1/N/N+1 case. Not derivable
        from the corpus as it stands.
    integer_overflow_and_allocation_budget:
      status: not_assessed
      note: Budget coverage is partly visible via reject_resource fixtures, but integer-overflow
        coverage cannot be separated without classifying fixtures. Deliberately not
        inferred from finding-code spelling.
    deep_nesting_and_non_progress:
      status: not_assessed
      note: 'Same: depth and non-progress fixtures exist for some families but are
        not distinguishable from the corpus without classification.'
    reference_semantics_cases:
      status: not_assessed
      note: No invariant in this family maps to a reviewed self-reference / overlap
        / duplicate-reference / cycle category. That may be correct for a non-reference-bearing
        family, or the mapping may be incomplete.
    stable_finding_and_offset_assertions:
      status: met
      note: Fixtures assert both finding codes and evidence locations.
      evidence:
      - malformed-contiguous_data_past_eof.yml
    no_activation_on_validation_failure:
      status: met
      note: Exact-build probe drives this family with external_open, plugin_load,
        write, network and crash forbidden; no rejecting fixture triggers one.
    differential_vs_legacy_on_valid_inputs:
      status: met
      note: h5policy-diff cross-checks every corpus file against libhdf5; this family
        has an accepting fixture in that set.
      evidence:
      - valid-userblock_earliest.yml
    in_memory_fuzz_targets:
      status: absent
      note: No fuzz_targets declared. h5policy-fuzz mutates whole-file seeds generically;
        this requires a dedicated target for the family.
    full_file_cve_and_ossfuzz:
      status: partial
      note: Full-file CVE fixture present; OSS-Fuzz integration does not exist in
        this repository.
      evidence:
      - cve-sizeof_size_uninit_length.yml
    lazy_validation_performance:
      status: partial
      note: h5policy-lazy shows validation cost flat while raw data grows 3031x, with
        a sensitivity control proving the counters respond to real metadata growth.
        Measured on the oracle as a whole, not per family, so it does not yet discharge
        this requirement family by family.
  chunk_index:
    truncation_every_byte_boundary:
      status: met
      note: h5policy-truncate walks every byte boundary of a seed contracted to this
        family, with no escape, internal error or hang.
      evidence:
      - h5policy/tests/valid/simple_dataset.h5
    count_and_extent_boundaries:
      status: not_assessed
      note: Requires classifying each fixture as a zero/max/N-1/N/N+1 case. Not derivable
        from the corpus as it stands.
    integer_overflow_and_allocation_budget:
      status: not_assessed
      note: Budget coverage is partly visible via reject_resource fixtures, but integer-overflow
        coverage cannot be separated without classifying fixtures. Deliberately not
        inferred from finding-code spelling.
    deep_nesting_and_non_progress:
      status: not_assessed
      note: 'Same: depth and non-progress fixtures exist for some families but are
        not distinguishable from the corpus without classification.'
    reference_semantics_cases:
      status: partial
      note: 'Categories covered: cycle. Not covered: duplicate_reference, overlap,
        self_reference.'
      evidence:
      - cycle
    stable_finding_and_offset_assertions:
      status: met
      note: Fixtures assert both finding codes and evidence locations.
      evidence:
      - malformed-bad_btree_v1_child_level.yml
      - malformed-bad_chunk_v2_btree_deep_checksum.yml
    no_activation_on_validation_failure:
      status: partial
      note: The probe forbids activation and the family is exercised, but a rejecting
        fixture still activates, crashes or hangs the build under test.
      evidence:
      - malformed-chunk_stored_size_below_framing.yml
      - malformed-chunk_zero_stored_size.yml
    differential_vs_legacy_on_valid_inputs:
      status: met
      note: h5policy-diff cross-checks every corpus file against libhdf5; this family
        has an accepting fixture in that set.
      evidence:
      - valid-chunk_fixed_array_partial_page.yml
      - valid-chunk_v1_btree_fletcher.yml
      - valid-chunk_v2_btree.yml
      - valid-simple_dataset.yml
      - valid-userblock_chunked.yml
    in_memory_fuzz_targets:
      status: absent
      note: No fuzz_targets declared. h5policy-fuzz mutates whole-file seeds generically;
        this requires a dedicated target for the family.
    full_file_cve_and_ossfuzz:
      status: absent
      note: No full-file cve/ fixture, and no OSS-Fuzz integration.
    lazy_validation_performance:
      status: partial
      note: h5policy-lazy shows validation cost flat while raw data grows 3031x, with
        a sensitivity control proving the counters respond to real metadata growth.
        Measured on the oracle as a whole, not per family, so it does not yet discharge
        this requirement family by family.
  dense_index:
    truncation_every_byte_boundary:
      status: met
      note: h5policy-truncate walks every byte boundary of a seed contracted to this
        family, with no escape, internal error or hang.
      evidence:
      - h5policy/tests/valid/empty.h5
    count_and_extent_boundaries:
      status: not_assessed
      note: Requires classifying each fixture as a zero/max/N-1/N/N+1 case. Not derivable
        from the corpus as it stands.
    integer_overflow_and_allocation_budget:
      status: not_assessed
      note: Budget coverage is partly visible via reject_resource fixtures, but integer-overflow
        coverage cannot be separated without classifying fixtures. Deliberately not
        inferred from finding-code spelling.
    deep_nesting_and_non_progress:
      status: not_assessed
      note: 'Same: depth and non-progress fixtures exist for some families but are
        not distinguishable from the corpus without classification.'
    reference_semantics_cases:
      status: partial
      note: 'Categories covered: duplicate_reference. Not covered: cycle, overlap,
        self_reference.'
      evidence:
      - duplicate_reference
    stable_finding_and_offset_assertions:
      status: partial
      note: Finding codes are asserted; no fixture pins an evidence location (expected_finding_evidence_locations).
      evidence:
      - malformed-bad_dense_bthd_checksum.yml
      - malformed-bad_dense_btlf_checksum.yml
      - malformed-bad_link_name_charset.yml
      - malformed-cache_image_unshadowed_btree-forensic.yml
      - malformed-dense_link_frhp_first_row_overflow.yml
      - malformed-dense_link_frhp_no_root_rows.yml
      - malformed-dense_link_frhp_width_not_power_of_two.yml
      - malformed-dense_link_frhp_width_zero.yml
      - malformed-filtered_dense_link_no_dblock_checksum.yml
      - malformed-filtered_dense_link_pipeline_overrun.yml
      - malformed-nul_compact_link_name.yml
      - malformed-over_capacity_v2_btree_leaf.yml
      - malformed-zero_v2_btree_record_size.yml
    no_activation_on_validation_failure:
      status: partial
      note: The probe forbids activation and the family is exercised, but a rejecting
        fixture still activates, crashes or hangs the build under test.
      evidence:
      - malformed-cache_image_unshadowed_btree-forensic.yml
      - malformed-dense_link_frhp_width_zero.yml
      - malformed-zero_v2_btree_record_size.yml
    differential_vs_legacy_on_valid_inputs:
      status: met
      note: h5policy-diff cross-checks every corpus file against libhdf5; this family
        has an accepting fixture in that set.
      evidence:
      - valid-dense_links.yml
      - valid-empty.yml
    in_memory_fuzz_targets:
      status: absent
      note: No fuzz_targets declared. h5policy-fuzz mutates whole-file seeds generically;
        this requires a dedicated target for the family.
    full_file_cve_and_ossfuzz:
      status: absent
      note: No full-file cve/ fixture, and no OSS-Fuzz integration.
    lazy_validation_performance:
      status: partial
      note: h5policy-lazy shows validation cost flat while raw data grows 3031x, with
        a sensitivity control proving the counters respond to real metadata growth.
        Measured on the oracle as a whole, not per family, so it does not yet discharge
        this requirement family by family.
  heap_structures:
    truncation_every_byte_boundary:
      status: partial
      note: The family's seed exceeds the sweep budget, so boundaries were strided
        rather than exhausted. A sampled sweep is not the every-boundary coverage
        this requires.
      evidence:
      - h5policy/tests/valid/dense_attribute.h5
      - h5policy/tests/valid/userblock_earliest.h5
    count_and_extent_boundaries:
      status: not_assessed
      note: Requires classifying each fixture as a zero/max/N-1/N/N+1 case. Not derivable
        from the corpus as it stands.
    integer_overflow_and_allocation_budget:
      status: not_assessed
      note: Budget coverage is partly visible via reject_resource fixtures, but integer-overflow
        coverage cannot be separated without classifying fixtures. Deliberately not
        inferred from finding-code spelling.
    deep_nesting_and_non_progress:
      status: not_assessed
      note: 'Same: depth and non-progress fixtures exist for some families but are
        not distinguishable from the corpus without classification.'
    reference_semantics_cases:
      status: partial
      note: 'Categories covered: cycle. Not covered: duplicate_reference, overlap,
        self_reference.'
      evidence:
      - cycle
    stable_finding_and_offset_assertions:
      status: met
      note: Fixtures assert both finding codes and evidence locations.
      evidence:
      - malformed-bad_dense_attr_btree_deep.yml
      - malformed-bad_fsinfo_page_size_over_max.yml
    no_activation_on_validation_failure:
      status: partial
      note: The probe forbids activation and the family is exercised, but a rejecting
        fixture still activates, crashes or hangs the build under test.
      evidence:
      - malformed-dense_attr_frhp_width_zero.yml
    differential_vs_legacy_on_valid_inputs:
      status: met
      note: h5policy-diff cross-checks every corpus file against libhdf5; this family
        has an accepting fixture in that set.
      evidence:
      - valid-dense_attribute.yml
      - valid-userblock_earliest.yml
    in_memory_fuzz_targets:
      status: absent
      note: No fuzz_targets declared. h5policy-fuzz mutates whole-file seeds generically;
        this requires a dedicated target for the family.
    full_file_cve_and_ossfuzz:
      status: absent
      note: No full-file cve/ fixture, and no OSS-Fuzz integration.
    lazy_validation_performance:
      status: partial
      note: h5policy-lazy shows validation cost flat while raw data grows 3031x, with
        a sensitivity control proving the counters respond to real metadata growth.
        Measured on the oracle as a whole, not per family, so it does not yet discharge
        this requirement family by family.
  shared_messages_legacy:
    truncation_every_byte_boundary:
      status: met
      note: h5policy-truncate walks every byte boundary of a seed contracted to this
        family, with no escape, internal error or hang.
      evidence:
      - h5policy/tests/valid/shared_attr_datatype.h5
    count_and_extent_boundaries:
      status: not_assessed
      note: Requires classifying each fixture as a zero/max/N-1/N/N+1 case. Not derivable
        from the corpus as it stands.
    integer_overflow_and_allocation_budget:
      status: not_assessed
      note: Budget coverage is partly visible via reject_resource fixtures, but integer-overflow
        coverage cannot be separated without classifying fixtures. Deliberately not
        inferred from finding-code spelling.
    deep_nesting_and_non_progress:
      status: not_assessed
      note: 'Same: depth and non-progress fixtures exist for some families but are
        not distinguishable from the corpus without classification.'
    reference_semantics_cases:
      status: partial
      note: 'Categories covered: cycle. Not covered: duplicate_reference, overlap,
        self_reference.'
      evidence:
      - cycle
    stable_finding_and_offset_assertions:
      status: met
      note: Fixtures assert both finding codes and evidence locations.
      evidence:
      - malformed-bad_sohm_btree_deep_checksum.yml
    no_activation_on_validation_failure:
      status: partial
      note: The probe forbids activation and the family is exercised, but a rejecting
        fixture still activates, crashes or hangs the build under test.
      evidence:
      - malformed-shared_message_self_reference.yml
      - malformed-shared_message_self_reference_attr.yml
      - malformed-sohm_frhp_width_zero.yml
    differential_vs_legacy_on_valid_inputs:
      status: met
      note: h5policy-diff cross-checks every corpus file against libhdf5; this family
        has an accepting fixture in that set.
      evidence:
      - valid-shared_attr_datatype.yml
      - valid-sohm_frhp_width8.yml
      - valid-userblock_latest.yml
    in_memory_fuzz_targets:
      status: absent
      note: No fuzz_targets declared. h5policy-fuzz mutates whole-file seeds generically;
        this requires a dedicated target for the family.
    full_file_cve_and_ossfuzz:
      status: absent
      note: No full-file cve/ fixture, and no OSS-Fuzz integration.
    lazy_validation_performance:
      status: partial
      note: h5policy-lazy shows validation cost flat while raw data grows 3031x, with
        a sensitivity control proving the counters respond to real metadata growth.
        Measured on the oracle as a whole, not per family, so it does not yet discharge
        this requirement family by family.
  message_envelope:
    truncation_every_byte_boundary:
      status: met
      note: h5policy-truncate walks every byte boundary of a seed contracted to this
        family, with no escape, internal error or hang.
      evidence:
      - h5policy/tests/valid/continuation_chunks.h5
    count_and_extent_boundaries:
      status: not_assessed
      note: Requires classifying each fixture as a zero/max/N-1/N/N+1 case. Not derivable
        from the corpus as it stands.
    integer_overflow_and_allocation_budget:
      status: not_assessed
      note: Budget coverage is partly visible via reject_resource fixtures, but integer-overflow
        coverage cannot be separated without classifying fixtures. Deliberately not
        inferred from finding-code spelling.
    deep_nesting_and_non_progress:
      status: not_assessed
      note: 'Same: depth and non-progress fixtures exist for some families but are
        not distinguishable from the corpus without classification.'
    reference_semantics_cases:
      status: not_assessed
      note: No invariant in this family maps to a reviewed self-reference / overlap
        / duplicate-reference / cycle category. That may be correct for a non-reference-bearing
        family, or the mapping may be incomplete.
    stable_finding_and_offset_assertions:
      status: met
      note: Fixtures assert both finding codes and evidence locations.
      evidence:
      - malformed-bad_attr_name_size_one.yml
      - malformed-bad_link_reserved_flags.yml
      - malformed-bad_link_type_invalid.yml
      - malformed-bad_object_header_message_count.yml
      - malformed-bad_ohdr_v1_message_unaligned.yml
      - malformed-bad_unknown_message_fail_always.yml
      - policy-unknown_message_skippable.yml
    no_activation_on_validation_failure:
      status: met
      note: Exact-build probe drives this family with external_open, plugin_load,
        write, network and crash forbidden; no rejecting fixture triggers one.
    differential_vs_legacy_on_valid_inputs:
      status: met
      note: h5policy-diff cross-checks every corpus file against libhdf5; this family
        has an accepting fixture in that set.
      evidence:
      - valid-continuation_chunks.yml
    in_memory_fuzz_targets:
      status: absent
      note: No fuzz_targets declared. h5policy-fuzz mutates whole-file seeds generically;
        this requires a dedicated target for the family.
    full_file_cve_and_ossfuzz:
      status: absent
      note: No full-file cve/ fixture, and no OSS-Fuzz integration.
    lazy_validation_performance:
      status: partial
      note: h5policy-lazy shows validation cost flat while raw data grows 3031x, with
        a sensitivity control proving the counters respond to real metadata growth.
        Measured on the oracle as a whole, not per family, so it does not yet discharge
        this requirement family by family.
  validation_controls:
    truncation_every_byte_boundary:
      status: partial
      note: The family's seed exceeds the sweep budget, so boundaries were strided
        rather than exhausted. A sampled sweep is not the every-boundary coverage
        this requires.
      evidence:
      - h5policy/tests/valid/fsm_persist.h5
    count_and_extent_boundaries:
      status: not_assessed
      note: Requires classifying each fixture as a zero/max/N-1/N/N+1 case. Not derivable
        from the corpus as it stands.
    integer_overflow_and_allocation_budget:
      status: not_assessed
      note: Budget coverage is partly visible via reject_resource fixtures, but integer-overflow
        coverage cannot be separated without classifying fixtures. Deliberately not
        inferred from finding-code spelling.
    deep_nesting_and_non_progress:
      status: not_assessed
      note: 'Same: depth and non-progress fixtures exist for some families but are
        not distinguishable from the corpus without classification.'
    reference_semantics_cases:
      status: not_assessed
      note: No invariant in this family maps to a reviewed self-reference / overlap
        / duplicate-reference / cycle category. That may be correct for a non-reference-bearing
        family, or the mapping may be incomplete.
    stable_finding_and_offset_assertions:
      status: met
      note: Fixtures assert both finding codes and evidence locations.
      evidence:
      - malformed-bad_fsm_section_list_checksum.yml
      - malformed-bad_superblock_v0_status_flags_high.yml
      - malformed-bad_superblock_v0_status_flags_reserved.yml
      - malformed-bad_superblock_v2_status_flags.yml
    no_activation_on_validation_failure:
      status: met
      note: Exact-build probe drives this family with external_open, plugin_load,
        write, network and crash forbidden; no rejecting fixture triggers one.
    differential_vs_legacy_on_valid_inputs:
      status: met
      note: h5policy-diff cross-checks every corpus file against libhdf5; this family
        has an accepting fixture in that set.
      evidence:
      - valid-fsm_persist.yml
    in_memory_fuzz_targets:
      status: absent
      note: No fuzz_targets declared. h5policy-fuzz mutates whole-file seeds generically;
        this requires a dedicated target for the family.
    full_file_cve_and_ossfuzz:
      status: absent
      note: No full-file cve/ fixture, and no OSS-Fuzz integration.
    lazy_validation_performance:
      status: partial
      note: h5policy-lazy shows validation cost flat while raw data grows 3031x, with
        a sensitivity control proving the counters respond to real metadata growth.
        Measured on the oracle as a whole, not per family, so it does not yet discharge
        this requirement family by family.
