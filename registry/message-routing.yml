# Finding messages that resolve to NO record family.
#
# This file is a MEASUREMENT, regenerated by `tools/message_routing.py --write`,
# not a hand-maintained list.  `tools/check_registry.py` recomputes it and fails
# on drift in either direction, the same contract libhdf5-evidence.yml uses:
#
#   * a message here that now routes  -> stale; someone fixed it, regenerate
#   * a message that routes to nothing and is NOT here -> a new gap
#
# Why it matters: a finding's message is the only per-occurrence discriminator a
# report carries, so `contexts` rules in findings.yml resolve a shared code's
# family from it.  For an `ambiguous` code, `resolve_finding` deliberately
# refuses to fall back to the entry's own `record`, so a message matching no
# rule names no family at all and `h5cve verify` cannot select a canary for it.
#
# Entries are a backlog, not an accepted state.  Emptying a code's list is the
# work; see the git history of findings.yml for H5_CORRUPT_OFFSET_OUT_OF_FILE,
# H5_CORRUPT_BAD_SIGNATURE and H5_CORRUPT_BAD_CHECKSUM as worked examples.

schema_version: 1

unrouted:
  H5_CORRUPT_BTREE_NODE_ENTRY_COUNT:
    - "SOHM v2 B-tree node record count exceeds its capacity"
    - "chunk B-tree node uses more entries than its superblock K permits"
    - "group B-tree node uses more entries than its superblock K permits"
    - "symbol table node uses more entries than its superblock K permits"
    - "v2 B-tree chunk node record count exceeds its capacity"
  H5_CORRUPT_FILTERED_HEAP_NO_DBLOCK_CHECKSUM:
    - "filtered SOHM heap disables direct-block checksums"
  H5_CORRUPT_FILTER_NELMTS_MISMATCH:
    - "decode filters disagree on the chunk element count"
  H5_CORRUPT_FRACTAL_HEAP_ID:
    - "dense attribute name index has a zero-length heap object ID"
    - "dense attribute name-index heap ID cannot be resolved in file bounds"
    - "dense creation-order heap ID cannot be resolved"
    - "dense creation-order index has a zero-length heap object ID"
    - "dense link name index has a zero-length heap object ID"
    - "dense link name-index heap ID cannot be resolved in file bounds"
  H5_CORRUPT_LENGTH_OVERFLOW:
    - "SOHM fractal heap header outside file"
    - "SOHM huge object outside file"
    - "SOHM list allocation outside file"
    - "SOHM list allocation size overflow"
    - "SOHM list checksum size overflow"
    - "SOHM list used size overflow"
    - "SOHM table outside file"
    - "SOHM v2 B-tree checksum offset overflow"
    - "SOHM v2 B-tree child-pointer bytes overflow"
    - "SOHM v2 B-tree header outside file"
    - "SOHM v2 B-tree node outside file"
    - "SOHM v2 B-tree node record bytes overflow"
    - "SOHM v2 B-tree node size overflow"
    - "VDS global heap collection outside file"
    - "VDS global heap header outside file"
    - "VDS global heap object size overflow"
    - "VDS hyperslab selection size overflow"
    - "VDS point selection size overflow"
    - "array datatype element count overflow"
    - "array datatype size calculation overflow"
    - "array dimension byte count overflow"
    - "attribute dataspace size overflow"
    - "attribute datatype size overflow"
    - "attribute name size overflow"
    - "chunk B-tree key size overflow"
    - "chunk B-tree node body outside file"
    - "chunk B-tree node capacity overflow"
    - "chunk B-tree node outside file"
    - "chunk B-tree node size overflow"
    - "chunk data outside file"
    - "chunk dimension byte count overflow"
    - "chunk element count overflow"
    - "compact data-size offset overflow"
    - "compact dimension byte count overflow"
    - "compact layout data offset overflow"
    - "compound member array storage size overflow"
    - "dataspace dimension byte count overflow"
    - "dense attribute v2 B-tree header outside file"
    - "dense attribute v2 B-tree internal node outside file"
    - "dense attribute v2 B-tree leaf outside file"
    - "dense attribute v2 B-tree leaf size overflow"
    - "dense attribute v2 B-tree node size overflow"
    - "dense creation-order heap object outside file"
    - "dense creation-order v2 B-tree header outside file"
    - "dense creation-order v2 B-tree node outside file"
    - "dense creation-order v2 B-tree node size overflow"
    - "dense link v2 B-tree header outside file"
    - "dense link v2 B-tree internal node outside file"
    - "dense link v2 B-tree leaf outside file"
    - "dense link v2 B-tree leaf size overflow"
    - "dense link v2 B-tree node size overflow"
    - "enum value byte count overflow"
    - "extensible array header outside file"
    - "extensible array index block body outside file"
    - "extensible array index block size overflow"
    - "extensible-array chunk data outside file"
    - "extensible-array data block outside file"
    - "extensible-array data block size overflow"
    - "extensible-array data-block page size overflow"
    - "extensible-array direct data-block geometry overflow"
    - "extensible-array direct-block address table overflow"
    - "extensible-array first secondary-block geometry overflow"
    - "extensible-array group offset overflow"
    - "extensible-array group span overflow"
    - "extensible-array page checksum bytes overflow"
    - "extensible-array page-init bitmap size overflow"
    - "extensible-array secondary block outside file"
    - "extensible-array secondary group geometry overflow"
    - "extensible-array secondary group offset overflow"
    - "extensible-array secondary group span overflow"
    - "extensible-array secondary-block address table overflow"
    - "extensible-array secondary-block geometry overflow"
    - "extensible-array secondary-block size overflow"
    - "external file list payload size overflows"
    - "external file list slot array size overflows"
    - "external file list slot size overflows"
    - "filter client-data byte count overflow"
    - "filter descriptor padding overflow"
    - "filter descriptor size overflow"
    - "fixed array data block body outside file"
    - "fixed array data block size overflow"
    - "fixed array header outside file"
    - "fixed array page size overflow"
    - "fixed array paged data block outside file"
    - "fractal heap direct block checksum field outside block"
    - "fractal heap direct block outside file"
    - "fractal heap header checksum size overflow"
    - "fractal heap header outside file"
    - "fractal heap header size overflow"
    - "fractal heap indirect block checksum size overflow"
    - "fractal heap indirect block size overflow"
    - "free-space manager header outside file"
    - "free-space section list outside file"
    - "group B-tree node capacity overflow"
    - "group B-tree node outside file"
    - "group B-tree node size overflow"
    - "hard link target address outside file"
    - "local heap header outside file"
    - "metadata cache image entry size overflow"
    - "metadata cache image outside file"
    - "metadata cache image parent list size overflow"
    - "object header outside file"
    - "object-header checksum size overflow"
    - "object-header continuation chunk outside file"
    - "object-header size overflow"
    - "shared-message object header outside file"
    - "shared-message object-header checksum size overflow"
    - "shared-message object-header size overflow"
    - "symbol table node capacity overflow"
    - "symbol table node entries outside file"
    - "symbol table node outside file"
    - "symbol table node size overflow"
    - "v2 B-tree chunk child-pointer bytes overflow"
    - "v2 B-tree chunk node checksum offset overflow"
    - "v2 B-tree chunk node record bytes overflow"
    - "v2 B-tree chunk node size overflow"
    - "v2 B-tree chunk-record dimensionality overflow"
    - "v2 B-tree header outside file"
    - "v2 B-tree node outside file"
  H5_CORRUPT_LINK_NAME_EMPTY:
    - "link name is empty"
  H5_CORRUPT_MESSAGE_FLAG_COMBINATION:
    - "dense attribute record has incompatible unknown-message flags"
    - "dense attribute record is both shared and marked do-not-share"
    - "dense attribute record was-unknown flag requires mark-if-unknown"
    - "object-header message has incompatible unknown-message flags"
    - "object-header message is both shared and marked do-not-share"
  H5_CORRUPT_MESSAGE_OVERRUNS_CHUNK:
    - "object-header message payload overruns chunk"
    - "v2 continuation chunk is too small for its signature and checksum"
  H5_CORRUPT_UNSUPPORTED_FRACTAL_HEAP_VERSION:
    - "fractal heap direct block has an unsupported version"
    - "fractal heap header has an unsupported version"
    - "fractal heap indirect block has an unsupported version"
  H5_CORRUPT_UNSUPPORTED_OBJECT_HEADER_VERSION:
    - "shared-message object header has an unsupported version byte"
    - "shared-message object header is neither v1 nor v2"
    - "v2 object-header signature has an unsupported version byte"
  H5_CORRUPT_V2_BTREE_RECORD_LAYOUT:
    - "dense attribute name-index record size is invalid"
    - "dense creation-order B-tree record size is invalid"
    - "dense link name-index record size is invalid"
    - "filtered v2 B-tree chunk record has no encoded chunk-size field"
    - "filtered v2 B-tree chunk-size field is wider than the file length encoding"
    - "unfiltered v2 B-tree chunk record size does not match the dataset rank"
    - "v2 B-tree chunk record size does not match the dataset rank"
  H5_CORRUPT_V2_BTREE_RECORD_SIZE:
    - "SOHM v2 B-tree record size is inconsistent with its type"
  H5_CORRUPT_V2_BTREE_ROOT_COUNT:
    - "leaf-root v2 B-tree total record count differs from its root count"
    - "v2 B-tree has records but no root node"
  H5_CORRUPT_V2_BTREE_SPLIT_MERGE:
    - "v2 B-tree split/merge percentages are inconsistent"
  H5_CORRUPT_V2_BTREE_SUBTREE_COUNT:
    - ""
    - "SOHM v2 B-tree subtree count is smaller than its node count"
  H5_RESOURCE_BTREE_DEPTH:
    - "SOHM v2 B-tree depth exceeds the selected profile limit"
    - "chunk B-tree depth exceeds the selected profile limit"
    - "chunk v2 B-tree depth exceeds the selected profile limit"
    - "dense attribute v2 B-tree depth exceeds the selected profile limit"
    - "dense creation-order v2 B-tree depth exceeds the selected profile limit"
    - "dense link v2 B-tree depth exceeds the selected profile limit"
    - "group B-tree depth exceeds the selected profile limit"
  H5_RESOURCE_SINGLE_VALUE_BYTES:
    - "attribute payload exceeds the selected profile limit"
    - "datatype storage size exceeds the selected profile limit"
    - "fill value exceeds the selected profile limit"
  H5_UNSUPPORTED_PICKLE_COVERAGE_GAP:
    - "chunk index traversal is bounded but not decoded yet"
    - "dense attribute name index uses a non-managed fractal-heap ID"
    - "dense attribute storage uses an unsupported fractal-heap/B-tree layout"
    - "dense creation-order index uses a non-managed fractal-heap ID"
    - "dense link name index uses a non-managed fractal-heap ID"
    - "dense link storage uses an unsupported fractal-heap/B-tree layout"
    - "driver-specific member files are not validated"
    - "nested shared object-header message resolution is not covered yet"
    - "non-file free-space manager section decoding is not covered yet"
    - "shared committed object header does not expose the requested message"
    - "shared object-header message is not stored in a committed object header"
    - "unknown object-header message is not classified by this validator"
    - "unsupported superblock length width"
    - "unsupported superblock offset width"
