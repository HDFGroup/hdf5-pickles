# A 300-chunk extensible array reaches beyond EAIB's inline records and direct
# pointers into an EASB and its nested EADBs.  Every checksummed metadata block
# must be traversed within the selected resource budgets.
file: valid/chunk_ext_array_deep.h5
profile: untrusted-strict
expected_decision: accept
expected_exit: 0
required_findings: []
forbidden: [crash, timeout, external_open, plugin_load, write]
