# Object-header NIL messages may be split into adjacent entries.  libhdf5
# accepts this as long as the NIL messages collectively leave no trailing chunk
# gap; h5policy must not require each individual NIL to cover the full remainder.
file: valid/adjacent_null_messages.h5
profile: untrusted-strict
expected_decision: accept
expected_exit: 0
required_findings: []
forbidden: [crash, timeout, external_open, plugin_load, write]
