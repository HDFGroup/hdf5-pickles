# Valid compact dataset storage: raw bytes live inside the object-header layout
# message.  Guards the compact-layout bounds check against false rejects.
file: valid/compact_layout.h5
profile: untrusted-strict
expected_decision: accept
expected_exit: 0
required_findings: []
forbidden: [crash, timeout, external_open, plugin_load, write]
