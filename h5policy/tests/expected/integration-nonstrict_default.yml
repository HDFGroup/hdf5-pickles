# The non-strict mapping default is independently selectable without enabling
# continued walking or the unreachable-metadata sweep.
file: valid/simple_dataset.h5
profile: untrusted-strict
profile_overrides:
  analysis_defaults:
    nonstrict_mapping: 1
expected_decision: accept
expected_exit: 0
required_findings: []
expected_mapping_mode: non_strict
forbidden: [crash, timeout, external_open, plugin_load, write]
