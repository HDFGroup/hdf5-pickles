# Preserve the old CLI spelling as a compatibility alias while reporting the
# option's actual semantics under the continue_after_rejection JSON key.
file: malformed/bad_btree_key.h5
profile: untrusted-strict
mode_flags: [--continue-after-corruption]
expected_decision: reject_corrupt
expected_exit: 2
required_findings: [H5_CORRUPT_HEAP_OFFSET_OUT_OF_BOUNDS]
expected_analysis:
  complete: true
  walk_started: true
  walk_completed: true
  stop_reason: ""
  continue_after_rejection: true
  findings_truncated: false
forbidden: [crash, timeout, external_open, plugin_load, write]
