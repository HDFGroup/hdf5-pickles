# A valid v1 file-space-info message using PAGE strategy with persistence
# disabled.  The message legitimately omits all twelve manager addresses; the
# manager graph remains a declared coverage gap, not structural corruption.
file: coverage/fsinfo_page_nonpersistent.h5
profile: untrusted-strict
expected_decision: unsupported_coverage_gap
expected_exit: 5
required_findings: [H5_UNSUPPORTED_PICKLE_COVERAGE_GAP]
forbidden_findings:
  - H5_CORRUPT_FSINFO_MESSAGE_TOO_SMALL
  - H5_CORRUPT_FREE_SPACE_PAGE_SIZE
forbidden: [crash, timeout, external_open, plugin_load, write]
