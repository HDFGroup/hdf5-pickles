# This external-file-list message claims one used slot but zero allocated slots.
# trusted-fast permits external storage, so the structural count invariant—not
# feature policy—must reject the file before any external access.
file: malformed/bad_efl_slot_count.h5
profile: trusted-fast
expected_decision: reject_corrupt
expected_exit: 2
required_findings: [H5_CORRUPT_EXTERNAL_FILE_LIST_SLOT_COUNT]
h5cve:
  # Family named mechanically from the primary finding via registry/findings.yml.
  family: external_file_list
  require_oracle_alignment: true
  allowed_statuses: [unexercised, verified]
forbidden: [crash, timeout, external_open, plugin_load, write]
