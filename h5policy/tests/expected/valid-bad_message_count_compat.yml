# v1 object header whose prefix under-declares the total message count only
# after following a continuation chunk.  This matches upstream HDF5's
# tbad_msg_count.h5 compatibility fixture; normal libhdf5 accepts it.
file: valid/bad_message_count_compat.h5
profile: untrusted-strict
expected_decision: accept
expected_exit: 0
required_findings: []
forbidden: [crash, timeout, external_open, plugin_load, write]
