# The file is repacked with shared dataspaces to force a list-backed Shared
# Object Header Message index.  Acceptance covers the SMTB/SMLI structures and
# resolution of the shared message from its managed fractal-heap object.
file: valid/sohm_list.h5
profile: untrusted-strict
expected_decision: accept
expected_exit: 0
required_findings: []
forbidden: [crash, timeout, external_open, plugin_load, write]
