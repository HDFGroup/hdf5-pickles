# A link-info message's size is inflated so it swallows following object-header
# messages.  The link-info payload has no legal trailing bytes.
file: malformed/bad_link_info_trailing_bytes.h5
profile: untrusted-strict
expected_decision: reject_corrupt
expected_exit: 2
required_findings: [H5_CORRUPT_LINK_INFO_TRAILING_BYTES]
forbidden: [crash, timeout, external_open, plugin_load, write]
