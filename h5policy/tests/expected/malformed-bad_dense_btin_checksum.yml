# This otherwise-valid dense-attribute index has one stored byte flipped in a
# BTIN internal-node checksum.  The node must be rejected before its records or
# child pointers are traversed.
file: malformed/bad_dense_btin_checksum.h5
profile: untrusted-strict
expected_decision: reject_corrupt
expected_exit: 2
required_findings: [H5_CORRUPT_BAD_CHECKSUM]
h5cve:
  family: heap_structures
  require_oracle_alignment: true
  allowed_statuses: [unexercised, violation]
forbidden: [crash, timeout, external_open, plugin_load, write]
