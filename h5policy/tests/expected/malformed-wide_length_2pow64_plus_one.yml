# Direct 16-byte contiguous-length fixture from the wide-length case bundle.
# The data address is valid, but the encoded length has a nonzero upper 64-bit
# limb, so h5policy must reject it as exceeding the validator's 64-bit file
# domain rather than treating the 16-byte width itself as unsupported.
file: ../../cases/GHSA-6mjw-q95m-vq87-37_sizeof_size_uninit_length/direct-wide-length-2pow64-plus-one.h5
profile: untrusted-strict
expected_decision: reject_corrupt
expected_exit: 2
required_findings: [H5_CORRUPT_LENGTH_EXCEEDS_U64]
forbidden: [accept, unsupported_coverage_gap, crash, timeout, external_open, plugin_load, write]
