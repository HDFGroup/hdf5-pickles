# Generated direct 16-byte contiguous-length fixture.  Its high limb is
# nonzero, but compatibility preflight declines the 16-byte width before that
# malformed value is decoded.
file: malformed/wide_length_2pow64_plus_one.h5
profile: untrusted-strict
expected_decision: unsupported_coverage_gap
expected_exit: 5
required_findings: [H5_UNSUPPORTED_PICKLE_COVERAGE_GAP]
forbidden: [accept, reject_corrupt, crash, timeout, external_open, plugin_load, write]
