# Generated direct 16-byte contiguous-length control.  The value itself is
# representable, but h5policy refuses the superblock's legal 16-byte length
# width for compatibility with libhdf5's current length decoder.
file: valid/wide_length_one.h5
profile: untrusted-strict
expected_decision: unsupported_coverage_gap
expected_exit: 5
required_findings: [H5_UNSUPPORTED_PICKLE_COVERAGE_GAP]
forbidden: [accept, reject_corrupt, crash, timeout, external_open, plugin_load, write]
