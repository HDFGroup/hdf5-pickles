# Generated direct 16-byte contiguous-length control.
# The upper 64-bit limb is zero and the low limb is 1, so the length is
# representable in h5policy's 64-bit file domain and the file should be
# accepted outright.
file: valid/wide_length_one.h5
profile: untrusted-strict
expected_decision: accept
expected_exit: 0
required_findings: []
forbidden: [crash, timeout, external_open, plugin_load, write]
