# Direct 16-byte contiguous-length fixture from the wide-length case bundle.
# The upper limb is zero, so the encoded length is narrowable into h5policy's
# uint64 domain, but the resulting data range still overflows the file bounds.
file: ../../cases/GHSA-6mjw-q95m-vq87-37_sizeof_size_uninit_length/wide-length-u64-max.h5
profile: untrusted-strict
expected_decision: reject_corrupt
expected_exit: 2
required_findings: [H5_CORRUPT_LENGTH_OVERFLOW]
forbidden: [accept, unsupported_coverage_gap, crash, timeout, external_open, plugin_load, write]
