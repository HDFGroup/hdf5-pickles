# Separate datasets exercise a legal enum over int32 and a legal VLen-of-int32
# datatype.  Their nested datatype encodings must pass recursive validation
# without being mistaken for malformed or over-depth types.
file: valid/enum_vlen.h5
profile: untrusted-strict
expected_decision: accept
expected_exit: 0
required_findings: []
forbidden: [crash, timeout, external_open, plugin_load, write]
