# The same ordinary six-int32 contiguous dataset is the legacy-profile baseline.
# Compatibility-oriented defaults do not change the clean acceptance of this
# fully self-contained file.
file: valid/simple_dataset.h5
profile: legacy
expected_decision: accept
expected_exit: 0
required_findings: []
forbidden: [crash, timeout, external_open, plugin_load, write]
