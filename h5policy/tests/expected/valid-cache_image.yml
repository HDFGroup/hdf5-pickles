# Generated through H5Pset_mdc_image_config, this is a real HDF5 metadata cache
# image. h5policy validates the container, authenticates it, and replays each
# cached body through the ordinary bounded metadata decoder.
file: valid/cache_image.h5
profile: legacy
expected_decision: accept
expected_exit: 0
h5cve:
  family: cache_image_dependency_graph
  allowed_statuses: [verified]
forbidden: [crash, timeout, external_open, plugin_load, write]
