# Generated through H5Pset_mdc_image_config, this is a real HDF5 metadata cache
# image.  h5policy validates its container and dependency envelopes but leaves
# opaque cached metadata bodies as an explicit coverage gap.
file: valid/cache_image.h5
profile: legacy
expected_decision: unsupported_coverage_gap
expected_exit: 5
required_findings: [H5_UNSUPPORTED_PICKLE_COVERAGE_GAP]
h5cve:
  family: cache_image_dependency_graph
  allowed_statuses: [verified]
forbidden: [accept, crash, timeout, external_open, plugin_load, write]
