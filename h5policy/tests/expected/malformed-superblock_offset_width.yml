# This v2 superblock advertises an absurd offset width (0x64). h5policy should
# reject it as an explicit bounded coverage gap before the dynamic-width
# superblock mapping can throw an internal out-of-bounds error.
file: ../../cases/ba3017135dfe62ae7b62ebc0cda6cb3f/input.h5
profile: untrusted-strict
expected_decision: unsupported_coverage_gap
expected_exit: 5
required_findings: [H5_UNSUPPORTED_PICKLE_COVERAGE_GAP]
h5cve:
  family: address_space_bounds
  allowed_statuses: [unexercised, verified]
forbidden: [accept, crash, timeout, external_open, plugin_load, write]
