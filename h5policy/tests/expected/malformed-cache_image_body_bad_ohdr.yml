# The authoritative cached object-header body is corrupt, while the MDCI
# envelope and checksum are valid. This guards the replay decoder against
# accidentally reading the stale file bytes at the same logical address.
file: malformed/cache_image_body_bad_ohdr.h5
profile: legacy
expected_decision: reject_corrupt
expected_exit: 2
required_findings: [H5_CORRUPT_UNSUPPORTED_OBJECT_HEADER_VERSION]
forbidden: [accept, crash, timeout, external_open, plugin_load, write]
