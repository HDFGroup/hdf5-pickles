# Fifty-one shared dataspaces force a genuine type-7 v2 B-tree with a BTIN
# root and BTLF children.  Every shared wrapper is resolved from its heap.
file: valid/sohm_btree.h5
profile: untrusted-strict
expected_decision: accept
expected_exit: 0
required_findings: []
forbidden: [crash, timeout, external_open, plugin_load, write]
