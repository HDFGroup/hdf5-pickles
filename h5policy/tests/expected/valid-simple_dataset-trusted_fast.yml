# The same ordinary six-int32 contiguous dataset is run under trusted-fast.
# With no external, filtered, or otherwise risky feature present, the more
# permissive profile still produces an unconditional accept.
file: valid/simple_dataset.h5
profile: trusted-fast
expected_decision: accept
expected_exit: 0
required_findings: []
forbidden: [crash, timeout, external_open, plugin_load, write]
