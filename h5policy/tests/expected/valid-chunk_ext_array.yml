# A v4 extensible-array chunk index backs a dataset with one unlimited
# dimension.  Acceptance covers the checksummed EAHD header, inline index block,
# chunk accounting, and each recorded chunk address.
file: valid/chunk_ext_array.h5
profile: untrusted-strict
expected_decision: accept
expected_exit: 0
required_findings: []
forbidden: [crash, timeout, external_open, plugin_load, write]
