# This otherwise-valid dense-link index has one stored byte flipped in a BTLF
# leaf checksum.  The leaf must be rejected before its link records and heap IDs
# are consumed.
file: malformed/bad_dense_btlf_checksum.h5
profile: untrusted-strict
expected_decision: reject_corrupt
expected_exit: 2
required_findings: [H5_CORRUPT_BAD_CHECKSUM]
forbidden: [crash, timeout, external_open, plugin_load, write]
