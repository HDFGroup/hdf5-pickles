# The sole SMLI record has an invalid location tag under a repaired checksum.
file: malformed/bad_sohm_list_location.h5
profile: untrusted-strict
expected_decision: reject_corrupt
expected_exit: 2
required_findings: [H5_CORRUPT_SOHM_LOCATION]
h5cve:
  # Family named mechanically from the primary finding via registry/findings.yml.
  family: shared_messages_legacy
  require_oracle_alignment: true
  # GENUINE gap: libhdf5 accepts on both the read AND the write path, so the
  # SOHM list location is never validated. See
  # registry/cases/sohm-index-divergences.yml.
  allowed_statuses: [violation]
forbidden: [crash, timeout, external_open, plugin_load, write]
