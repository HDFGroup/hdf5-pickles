# A group whose dense-link name v2 B-tree is depth >= 2; the dense-link walker
# handles depth 0 and 1 -> "not yet classified".  Flips to accept when deeper
# v2 B-tree internal nodes are descended.
file: coverage/dense_links_deep.h5
profile: untrusted-strict
expected_decision: unsupported_coverage_gap
expected_exit: 5
required_findings: [H5_UNSUPPORTED_PICKLE_COVERAGE_GAP]
forbidden: [crash, timeout, external_open, plugin_load, write]
