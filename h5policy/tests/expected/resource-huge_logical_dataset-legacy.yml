# The 128 GiB logical dataset that untrusted-strict rejects on its resource
# budget is accepted under legacy, whose budgets are effectively unlimited.
file: resource/huge_logical_dataset.h5
profile: legacy
expected_decision: accept
expected_exit: 0
required_findings: []
forbidden: [crash, timeout, external_open, plugin_load, write]
