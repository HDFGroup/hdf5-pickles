# Group-info flags define the exact payload size.  An inflated size swallows
# following object-header bytes and is rejected by libhdf5.
file: malformed/bad_group_info_trailing_bytes.h5
profile: untrusted-strict
expected_decision: reject_corrupt
expected_exit: 2
required_findings: [H5_CORRUPT_GROUP_INFO_TRAILING_BYTES]
forbidden: [crash, timeout, external_open, plugin_load, write]
