# Two thousand allocated one-element chunks force a fixed-array data block into
# paged form.  Acceptance covers the page-initialization bitmap, per-page
# checksums, and chunk records that cross page boundaries.
file: valid/chunk_fixed_array_paged.h5
profile: untrusted-strict
expected_decision: accept
expected_exit: 0
required_findings: []
forbidden: [crash, timeout, external_open, plugin_load, write]
