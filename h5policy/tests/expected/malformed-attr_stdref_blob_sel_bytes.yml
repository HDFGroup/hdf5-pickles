# A revised reference declaring more selection bytes than its blob holds.
#
# libhdf5's own safety check, and an exact one: H5R__decode_region adds the two
# u32 header words to the stored size and refuses on `*nbytes < buf_size`. So the
# rule here is that equality, not a heuristic. Measured -- H5Aread fails cleanly.
# Case record: registry/cases/attribute-selection-unvalidated.yml
file: malformed/attr_stdref_blob_sel_bytes.h5
profile: untrusted-strict
expected_decision: reject_corrupt
expected_exit: 2
required_findings: [H5_CORRUPT_REFERENCE_BLOB_EXTENT]
h5cve:
  # Named mechanically from the primary finding via registry/findings/; the
  # selection codes are shared with the VDS layout and route on the message's
  # role prefix, so an attribute-borne selection resolves to message_envelope.
  family: message_envelope
  require_oracle_alignment: true
  allowed_statuses: [unexercised, verified]
forbidden: [crash, timeout, external_open, plugin_load, write]
