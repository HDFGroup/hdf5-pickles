# The BTIN checksum is repaired after its first child pointer is moved beyond
# EOA, isolating recursive child-range validation from checksum handling.
file: malformed/bad_chunk_v2_btree_child_address.h5
profile: untrusted-strict
expected_decision: reject_corrupt
expected_exit: 2
required_findings: [H5_CORRUPT_OFFSET_OUT_OF_FILE]
forbidden: [crash, timeout, external_open, plugin_load, write]
