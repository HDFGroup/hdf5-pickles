# The MDCI payload is otherwise valid, but its trailing Jenkins checksum does
# not authenticate the cached bodies. The replay pass must not start.
file: malformed/cache_image_bad_checksum.h5
profile: legacy
expected_decision: reject_corrupt
expected_exit: 2
required_findings: [H5_CORRUPT_BAD_CHECKSUM]
forbidden: [accept, crash, timeout, external_open, plugin_load, write]
