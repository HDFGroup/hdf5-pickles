# A fixed-shape dataset with four chunks uses the v4 fixed-array index.  The
# checksummed FAHD header and FADB data block, chunk count, and all four chunk
# addresses must validate without findings.
file: valid/chunk_fixed_array.h5
profile: untrusted-strict
expected_decision: accept
expected_exit: 0
required_findings: []
forbidden: [crash, timeout, external_open, plugin_load, write]
