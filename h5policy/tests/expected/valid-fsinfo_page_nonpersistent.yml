# A v1 file-space-info message using PAGE strategy with persistence disabled.
# The message legitimately omits all twelve manager addresses, so there is
# nothing to walk: the envelope is validated and the file is a clean accept
# (previously a coverage gap under the Phase 2 stub).
file: valid/fsinfo_page_nonpersistent.h5
profile: untrusted-strict
expected_decision: accept
expected_exit: 0
required_findings: []
forbidden_findings:
  - H5_CORRUPT_FSINFO_MESSAGE_TOO_SMALL
  - H5_CORRUPT_FREE_SPACE_PAGE_SIZE
  - H5_UNSUPPORTED_PICKLE_COVERAGE_GAP
forbidden: [crash, timeout, external_open, plugin_load, write]
