# The first EADB checksum is damaged while its EAHD and EAIB remain valid.
# Root-only extensible-array validation must not accept this file.
file: malformed/bad_ext_array_deep_checksum.h5
profile: untrusted-strict
expected_decision: reject_corrupt
expected_exit: 2
required_findings: [H5_CORRUPT_BAD_CHECKSUM]
forbidden: [crash, timeout, external_open, plugin_load, write]
