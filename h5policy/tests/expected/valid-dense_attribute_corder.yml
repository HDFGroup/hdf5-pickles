# Dense attributes with both the type-8 name index and type-9 creation-order
# index.  Both trees and their shared heap-ID set must validate.
file: valid/dense_attribute_corder.h5
profile: untrusted-strict
expected_decision: accept
expected_exit: 0
required_findings: []
forbidden: [crash, timeout, external_open, plugin_load, write]
