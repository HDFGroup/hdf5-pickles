# Compact link declares an ASCII name but contains a high-bit byte.  h5py/libhdf5
# rejects this during hard-link traversal.
file: malformed/bad_link_name_charset.h5
profile: untrusted-strict
expected_decision: reject_corrupt
expected_exit: 2
required_findings: [H5_CORRUPT_LINK_NAME_CHARSET]
forbidden: [crash, timeout, external_open, plugin_load, write]
