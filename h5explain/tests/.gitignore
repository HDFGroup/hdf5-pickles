# Generated navigation fixtures (produced by make_fixtures.py via run.sh).
# The tracked specification is test_h5explain.py; the binaries are build output.
fixtures/
# Private copy of the h5policy corpus for the check cases (h5policy-gencorpus
# via run.sh), kept out of h5policy/tests so the suites cannot race.
corpus/
__pycache__/
